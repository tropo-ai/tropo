# v1.90 pre-ship validator baseline — 2026-08-21

*Recorded by Metis G109. The gap this closes: G108's transfer named it explicitly — "the
studio still records no validator baseline at ship," which is why A152's sweep-attribution
counts were unprovable last week. This file is the comparison point for Sunday's ship and
the post-ship retrospective.*

## The numbers (full `tropo-validate.py` run, evening 2026-08-21, three days pre-ship)

```
Summary: 79 passed, 175 failed, 8677 warnings, 0 normalizable
```

Reference points earlier the same day:
- morning (my boot, ~00:40): 80 passed, 163 failed, 8496 warnings
- the drift across one heavy build day: +12 failed checks, +181 warnings — the cost of a
  day where three agents landed ceremony replacement, nine closes, two builds in flight,
  and one agent turnover. Whether Sunday ships above or below this line is the honest
  measure; matching the morning line exactly is not the goal.

## How to use on ship day

Run the validator immediately before `fire` and immediately after `verify-live`; record
both summaries beside this baseline in the release retrospective. A class that grew is
either release debt (name it) or a new rule's backlog (route it). No hand-tuning the
corpus to flatter the number — the number exists to be honest, not green.
