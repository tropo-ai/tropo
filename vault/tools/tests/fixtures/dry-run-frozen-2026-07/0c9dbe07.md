---
uid: 0c9dbe07
type: test-spec
title: v1.78 — Clean Self-Update — Test-Spec (the floor test, the covenant gauntlet, and the Po walk)
description: 'Test-spec paired with dev-spec fc4874f4 (Gate-2 clean self-update, LOCKED + covenant-complete per ADR-049 eee2246e). Center of gravity, three proofs: (1) THE FLOOR TEST — seed a throwaway
  studio with user slug-uid content, apply a real update replacing tropo-* components, assert by mtime+hash that ZERO user files changed AND every targeted OS component updated (fails if a single user file
  is touched); (2) THE COVENANT GAUNTLET — plant a covenant-violating op in an update package and watch the BUILD refuse (ADR-049 layer 2 is a blocking gate, proven by plant, not asserted); (3) THE PO WALK
  — in a seeded stranger studio, the concierge runs the full arc cold: discover -> present dry-run diff -> explicit approval -> apply -> present the receipt in plain language. Everything verifies against
  REAL runs (a real update package, a real apply, a real receipt) — never the relay, never a summary.'
status: done
stage: done
state: active
target_substrate:
- 71f186cf
- f1d4b9e6
- b8f2d6a3
target_subsystem: null
triggering_dev_spec: fc4874f4
cycle_class: engine-runtime
coverage_class: regression
manual_walk_percentage_ceiling: 30
acceptance_evidence:
  - 'Mechanical sweep test_gate2_clean_update_sweep.py: 15 passed / 0 failed / 1 skipped (the Po walk, agentic) — run independently by Talos T23, Talos T24, and Argus A123 (2026-07-02); behavior 1 round-trips the REAL v1.77 MANIFEST.md (957 rows)'
  - 'Floor test test_clean_update_floor.py: PASS both modes incl. LOCK-AMENDMENT 3 e2e (receipt-last seal proven, dual-hash content identity, confirm hard-stop resists blanket approval) + gauntlet catches the planted violation; verified against the REAL round-1 failed-walk artifacts, not fixtures'
  - 'THE PO WALK (behavior 12): round 1 FAIL (receipt-then-mutate + confirm bypass — remediated same day via LOCK-AMENDMENT 3); round 2 PASS in a pristine seeded v1.77 studio — cold unprompted discovery, hard-stops HELD (Po escalated the de794a03 seam instead of self-resolving), Mike resolved per-file live, receipt verified by Argus against out-of-studio pre-boot fingerprints: 5/5 user files byte-identical, honest 3-file chrome disclosure. Verdict artifact: tropo-releases/v1.78.0/cold-walk-verdict.json (overall PASS)'

references_cycle_brief: eee2246e
acceptance_criteria: 'Coverage is real, not box-checking: a reviewer can watch the floor test seed user files, apply a real update, and prove zero user-file churn by hash; can plant a covenant-violating
  op and watch tropo-build-release REFUSE to build; can curl the discovery manifest and get all three response shapes (up-to-date / updates-available / migration_required) plus graceful-on-garbage; can
  point a below-minimum studio at an update and watch the halt deliver the manifest message VERBATIM with .tropo/version.md untouched; can grep the reconciled playbook for zero system/updates/ refs and
  zero SHA-drift block; can read a per-apply receipt and verify their own file hashes against it; and can watch Po (a cold concierge boot) run the whole arc without touching the machinery by hand.'
behaviors_covered:
- behavior_description: 'THE FLOOR TEST: seed a throwaway studio with user <slug>-<uid> and bare <uid>.md content (record mtime+hash each); apply an update package whose ops replace tropo-* components;
    assert ZERO user files changed (mtime AND content hash identical) AND every targeted tropo-* component is at the new version. The test FAILS if a single user file is touched.'
  test_substrate_path: vault/playbooks/clean-update-floor-test (Talos mints tropo-/uid at build)
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 9
- behavior_description: 'THE COVENANT GAUNTLET (ADR-049 layer 2, adversarially isolated): with the floor test wired as a blocking build gate, plant an update op that writes a user-content path; tropo-build-release
    REFUSES to build; remove the plant; the build passes. The gate — not a side effect — prevents the bad outcome.'
  test_substrate_path: vault/tools/tropo-build-release.py
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 12
- behavior_description: 'Namespace predicate is deterministic and content-blind: feed the apply engine a mixed set (tropo-foo.md, bar-1a2b3c4d.md, 1a2b3c4d.md, a .tropo/ bootstrap-floor file); only tropo-*
    and the bootstrap floor mark REPLACE; user content marks PRESERVE; no content read occurs in the decision.'
  test_substrate_path: 71f186cf
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 1
- behavior_description: 'Playbook reconciliation complete: grep canonical 71f186cf returns zero system/updates/ hits and zero SHA-drift/seed-propagate block; the namespace-predicate Rule is present citing
    ca6e5824; version reads v2.0.'
  test_substrate_path: 71f186cf
  verification_method: structural_check
  verifies_acceptance_criterion: 2
- behavior_description: 'Update discovery contract: the manifest at the releases bucket yields all three shapes — up-to-date (updates:[]), updates-available (list with stable non-expiring URLs), migration_required
    (true + plain-language message); a garbage version yields a graceful well-formed response; with no network the agent skips silently and proceeds (no error state).'
  test_substrate_path: f1d4b9e6
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 3
- behavior_description: 'Apply-time incompatibility halt: point a below-minimum_supported studio at an update; the agent delivers the manifest message VERBATIM and halts BEFORE any operation; .tropo/version.md
    unchanged; nothing partially applied.'
  test_substrate_path: b8f2d6a3
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 5
- behavior_description: 'State machine re-homed: pending/applied/failed + the update-history index live under vault/updates/; apply-update reads/writes only that location; no code path references the dissolved
    system/updates/.'
  test_substrate_path: vault/updates/
  verification_method: structural_check
  verifies_acceptance_criterion: 6
- behavior_description: 'v1.1 mechanisms intact verbatim: dry-run-diff + approve-on-diff + commit-divergence-halt fire correctly; defensive log-and-skip yields PASS-with-skips on unexpected structure; a
    second apply of the same migration produces zero changes (idempotent); version-gap guidance names intermediate versions in order; NO auto-rollback (partial state left inspectable).'
  test_substrate_path: 71f186cf
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 7
- behavior_description: 'THE RECEIPT (ADR-049 layer 3): every apply writes a verification receipt to the vault/updates/ history — pre/post hash manifest of user substrate + the list of tropo-* components
    replaced; a reader can verify their own files against it without trusting the tool; Po presents it in plain language at apply end.'
  test_substrate_path: 71f186cf
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 13
- behavior_description: 'Concierge route reconciled: grep .tropo/concierge/activate.md returns zero system/updates/ hits; with an update staged in vault/updates/pending/, a cold concierge boot detects and
    surfaces it after the greeting in plain language; a migration_required manifest is delivered VERBATIM; nothing applies without explicit owner approval.'
  test_substrate_path: .tropo/concierge/activate.md
  verification_method: agentic_review
  verifies_acceptance_criterion: 11
- behavior_description: 'THE PO WALK (experience proof, complements the mechanical floor test): in a seeded stranger studio, the concierge (Po''s boot path) runs the full arc cold — discover the update
    -> present the dry-run diff -> capture explicit approval -> apply -> confirm zero user-file churn -> present the receipt and walk the owner through what changed. A customer never touches the machinery
    directly.'
  test_substrate_path: .tropo/concierge/activate.md
  verification_method: agentic_review
  verifies_acceptance_criterion: 12
- behavior_description: 'Reference migrations + vault-steward revalidated against the single vault/ tree: scopes resolve under vault/; no migration writes a tropo-* OS component or .tropo/; steward regression
    checks pass; no system/updates/ path assumed.'
  test_substrate_path: .tropo/playbooks/migrations/
  verification_method: machine_executable_script
  verifies_acceptance_criterion: 8
owner: argus
author: argus-a122
created: '2026-07-02'
created_by: argus-a122
target_release: '1.78'
member_of:
- 90798e6b
refs:
- fc4874f4
- eee2246e
schema_version: 2
governed_by: 8dd772a0
triggered_by_dev_cycle: b6a8cd32
---

# v1.78 — Clean Self-Update — Test-Spec

*Paired with dev-spec [fc4874f4](fc4874f4.md) + the covenant [ADR-049](eee2246e.md). Pre-authored standalone by Argus A122 (2026-07-02) for claim at ceremony time (orphan-claim path, proven twice this session). The three proofs that earn the cost: the floor test (zero churn by hash), the covenant gauntlet (the build refuses a planted violation), and the Po walk (a customer never touches the machinery). `verifies_acceptance_criterion` numbers map to fc4874f4''s acceptance_criteria order.*
