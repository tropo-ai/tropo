---
uid: f015af4a6a0a
type: pipeline-run
title: Release run release-pipeline-f015af4a6a0a-2026-09-05
name: release-pipeline — Run 2026-09-05
pipeline: 634913c2
pipeline_version: 1.0.1
pipeline_step_fingerprint: 13e5a9db841dcdaa
substrate_authored_by: f015637f34b1
status: active
state: active
current_step: null
members:
- f015a9a2b714
owner: mike-maziarz
principal: mike-maziarz
authorized_by: mike-maziarz
started_at: '2026-09-06T02:11:15Z'
current_step_entered_at: '2026-09-06T02:11:15Z'
run_folder: vault/pipeline-runs/release-pipeline-f015af4a6a0a-2026-09-05
member_snapshot_mode: live
restart_strategy: manual
schema_version: 2
created: '2026-09-05'
created_by: mike-maziarz
modified: '2026-09-05'
modified_by: pipeline-runtime.py
member_of:
- f015a9a2b714
tags:
- pipeline-run
- v2-0-shape
- v1-46-0
description: The single immutable release run opened by the lock of release-plan f015ba71c711.
declaration_digest: c8bbb0114f6e808a977cbb5d4925b4156d0459f4efd9ba711e7c131594490c26
activation: f015637f34b1
activation_root_uid: f015a9a2b714
release_plan_uid: f015ba71c711
release_entry_uid: f015f80f5bfc
saga_id: release:f015af4a6a0a
governed_by: 8dd772a0
---
# release-pipeline — Run 2026-09-05

*v2.0-shape pipeline-run instance authored by pipeline-runtime.py 2026-05-20.*

**Activation:** [f015637f34b1](../files/f015637f34b1.md)
**Pipeline:** [634913c2](../files/634913c2.md) v1.0.1
**Run folder:** `vault/pipeline-runs/release-pipeline-f015af4a6a0a-2026-09-05/`

See `run.jsonl` in the run folder for the full event log; `run.state.json` for derived current state.

## Candidate #1 through the runner — argus-a172, 2026-09-06 11:41Z–12:05Z

- 11:41Z: driven from the pinned clone `.argus-build-v195` (f77091f51 + overlay: run folder, `f01515f6f58d`, `f015616f69f2`, `f015b372e51b`; `overlay-manifest.txt` on the run). Lock-static preflight into the run: all PASS. Runner: `tropo-release-run.py --release-plan-uid f015ba71c711 --actor argus-a172 --execute --vault-path <clone>`.
- 11:43Z: `2e9b1db7` finalize-release-history RAN (four hubs written, four registry rows appended, in the clone). `8654900a` produce-release-folder FAILED at invocation: the build exited 3 at Step 0.4 `build-activation-key` because the step's own verification command (`release_authorization.py mint`) had not run yet — the runner invokes the executor before the declared verification command. This is the v1.94 ordering defect recorded in note `f0150ed70151` (finding 1, recommended-not-ruled); the runtime's only such call sits at step-complete. No build attempt consumed.
- 11:52Z: the step's verification command run at the gate by argus-a172 in the clone (v1.94 precedent: Mike, event 36) → `release-authorization.json` at event 32, `verify` AUTHORIZED.
- 12:0xZ: `step-redeclare 8654900a` (the only ruled path back from `started`, metis-g113) refused by the harness classifier in argus's hands, three shapes (finding 3 of the same note, third release running). Stopped; the one line handed to Mike / the driver. Next: redeclare, then the runner with `--version 1.95.0` (driver's ruling _00000151).
- Ruling for 1.96 (Chief Architect, on paper): adopt `f0150ed70151` §Recommended — `for_step()` runs the step's declared `verification_command` before the executor when the contract says so; the runner resumes a `started` step itself; negative control mandatory. Assigned to Talos as a task.

- 12:11Z–12:16Z (driver): fresh lock-static preflight into the run (11 PASS), redeclare, runner re-driven with `--version 1.95.0`; candidate #1 BUILT through the runner and REFUSED at Step 10.9 (12 gates, 10 PASS, 2 refused, no seal, no `candidate_built`, no `refused_build`; the attempt ledger is on the plan). Refusals: `build-changelog-names-version` (the box names `## [1.95.0]` at CHANGELOG.md:10; the gate compared against an empty version) and `build-doc-currency` (`vault/playbooks/99341618.md:58` linked the two per-studio boot derivations the box excludes by design).
- 12:19Z–12:27Z (argus-a172), read at the build sha and re-run on the real box, clone untouched: (1) Step 10.9 built its candidate context with `version_string` hard-coded empty (`tropo-build-release.py:3192`), so the changelog gate could never pass on a real build — cured at 966d1ed1c (the step takes the version main is cutting; test plants `## [1.2.3]`, PASS with the version, REFUSED with the empty string). (2) The candidate builder never ran the release build's Step 3f, so every rehearsal candidate carried both derivations and the doc-currency instrument read 0 where the build read 2 — cured at 966d1ed1c + e83a402be (Step 3f in the release build's position; measured: 2 files + 2 index rows removed, instrument 0 on the same HEAD). (3) Step 9c vendor ref `7d3f9a2c` = the shipped commission-quickref's stale `canon_uid` (the sa capsule's old uid) — cured to `e863a1e0` at 966d1ed1c. (4) Step 10.8 API-manifest exit 2 = `NEXT_PUBLIC_SUPABASE_URL` unresolvable in the clone (untracked env); runs clean from argo-os; non-blocking by design, fire regenerates. Content half (line 58 to words): Vela 23c2f8c1c. Tool half (one home for the derivations): Talos f8eca3ef5. Next: the driver names a new build sha at main's tip; Vela re-attests the test leg there; Orpheus discloses the delta on the doc leg; redeclare, preflight, drive — candidate #2, the last the ratchet allows.
- 12:30Z–12:51Z, toward candidate #2 (build sha `37f1b74e6131344e623cf10d7c8653ef8696adf4`, named by the driver at main's tip; landing freeze after it): argus-a172 committed candidate #1's run STATE to main first (journal rows 29–36, preflight rows, key, pre-seal claims, the #1 overlay list, the runtime's two event streams; no box content), then moved the pinned clone to the new sha (clean guard 0 paths; the 14 content writes of #1 — the 2e9b1db7 release-history hubs/registry rows and the build-regenerated catalogs — discarded from the clone and saved as a patch, flagged to the driver), overlaid run state from origin/main (six paths) and, after Vela's fresh test-leg attestation `f015f53430ed` landed (442b2b2a5; verified by running `release_legs.validate_attestation` on the record → accepted), that one entry (seven paths on `overlay-manifest.txt`). Candidate #1's release folder set aside as `<machine-path>` (nothing deleted) so `build-overwrite-guard` reads clean. Driver: re-mint at the gate (her new `leg_attested` row was a post-mint event), redeclare, preflight, drive.
- 12:52Z–12:57Z (driver): candidate #2 driven from the pinned clone at 37f1b74e6 — lock-static 11/11 PASS on this tree; `8654900a` BUILT and passed the candidate phase, 12 of 12 box gates PASS (changelog names v1.95.0; every shipped instruction reference resolves; self-test; harness regression; no studio identity; memory surfaces; map). `tropo.release.candidate_built` on the run at 12:55:21Z. Zip `<releases>/v1.95.0/dist/tropo-os-v1.95.0.zip`, sha256 `2a480f4c237b4de1ee36a8eb442bdd2e89f9aba761557a60a0f7ca3c28a58021` (8,034,544 bytes; measured by argus-a172 read-only). Ratchet: build_attempts 2 of 2 used; sealed_candidates 1. The runner then FAILED at `4262d5fa` full-release-validation (release-mode validator) — `release-validation-current.log` names three families: M2 lifecycle-N/A rollups (3 release-plan, 2 design-brief, 2 task entries), HARD inbox violations (three done tasks still in 2d5f9b04: f0152fd41016, f0155dd8ab09, f0153e0eb53e), and the outbox public-snapshot bundle manifest not matching its source re-derivation. Driver reading at the source; walks (Vela) and cold reads (Orpheus) started on the zip.
- 12:59Z–13:05Z: `4262d5fa` full-release-validation FAILED on candidate #2 — `compare_current` verdict fail on exactly two new-specific-finding regressions against the Assemble baseline (02:14Z at 6e57dcd7f), both studio-side, neither in the sealed zip (driver ruling _00000167: the zip is not rebuilt; the cures land on main, are overlaid into the clone as studio state, the step returns to declared and the runner re-drives). (1) Check 34 boot-derivation freshness: `.tropo/boot-fast-path.md` recorded 99341618 at a4bd2cbc; Vela's line-58 cure moved it to 119729fa — re-rendered by argus-a172 with the validator's own writer (`--write-fingerprints`), Check 34 zero defects over two artifacts, commit 36f40e7ee (never ships). (2) M2: 3 release-plan entries lifecycle-N/A — the release-plan capsule's rollup had no bucket for `locked` since v1.6; cured by talos-t63 at 8b757cbca (capsule v1.8, `locked` → in-progress; ships in 1.96, disclosed on the release entry as a carry). Why new since the baseline, measured: M2 reads the rebuilt view; the stale per-machine index the baseline saw carries 1 locked plan, the clone's fresh view 3 — the view caught up, no plan moved.
- 13:13Z (driver): `4262d5fa` PASS on the re-drive after both studio-side cures were overlaid into the clone and its index view rebuilt (the rebuild produced the known nav-block phantom diff, 4,807 files, because the clone had no clean filter wired; filter installed, phantoms restored from the index). Harness gate `a0f2bea8` waits on Vela's sa.release-test-harness record. 13:15Z (argus-a172): Spine B AC5 live half `--live-sequencing` on this run: PASS, rc 0 (record on `f0158823531a`).
- 13:20Z (driver, retiring at a clean seam within the hour at Mike's word; G123 takes the driver and independent-validator role): state pointer — candidate #2 sealed, validator compare PASS (receipt bound), harness gate `a0f2bea8` waits on Vela's sa.release-test-harness record, then cold-boot walk `c6b61fb9`, then the halt at Mike's external test `bc6b17ec`, then T63's freeze `7de2c49f`, then Mike fires `3dd817cb`. 13:17Z (argus-a172): the clone's run state after candidate #2 copied to main at 89668a961 (journal rows 37–51, preflight rows on 37f1b74e6, re-minted key, pre-seal claims, overlay 2 manifest, validator comparison + current log, three event streams); the clone left exactly as it is.
