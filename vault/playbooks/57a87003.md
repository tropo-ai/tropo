---
uid: 57a87003
type: playbook
title: Set Up My Team
version: '1.0'
status: draft
owner: argus
readers:
  - agent
scope: single-session
estimated_duration: 15 minutes
tags:
  - onboarding
  - concierge-paths
  - team-setup
  - multi-agent
  - governance
  - v1.3
trigger: user-intent-setup-team
domain: onboarding
governed_by: e7b3c509
spec: e6d373bc
calls: -vault/skills/tropo-create-executive-agent.md
author:
  name: Argus A31
  role: Chief Architect
created: 2026-04-21
modified: 2026-04-21
created_by: argus-a31
extraction_scope: ship
concierge_path: true
subsystem_hub:
  - 76bab75f
---

# Set Up My Team

> **How this works.** The AI reading this playbook drives the configuration — it asks the questions, authors the files, and waits for your approval at each step. You're guiding; the AI is typing. **You don't need to read past this line** — your AI does. Skim if you're curious; otherwise let the AI handle it.

*The founder says: "I want multiple agents that coordinate." Could be a solo founder building a personal crew, a consultant assembling 2-3 specialists, or a small company setting up a governed team. The name says "team" deliberately — not "company" — because the same shape scales from one founder with three agents to a 50-person operation. This playbook creates 2–3 agents and customizes the operating agreement so their coordination has governance. Coordination itself needs no wiring — agents talk through the typed event log from the moment they exist.*

---

> ## ✅ PAIR-CHANNEL MECHANIC REMOVED — 2026-08-26
>
> **The retired mechanic this banner warned about has been taken out of the
> playbook.** Removed by argus-a159 on Mike's ruling: *"we don't use channels/
> anymore. it is deprecated. we use events subsystem."* Every instruction to
> create pair channels, maintain a channel index, or log to `channels/ops.md`
> is gone; what replaced them is nothing, because coordination needs no artifact.
> The banner is kept — rather than deleted — because the history below is the
> most useful thing in this file.
>
> **What is current:** agents coordinate through the typed event log, addressed
> by party UID. Emit with `tropo-emit-event.py --as <agent> --subject
> <party-uid>`; drain with `tropo-check-events.py --as <agent>`; crew-wide
> notices are `tropo.broadcast.crew` events. A reply clears `reply_required`
> only when correlated to the original event ID. There are no pair channels to
> create, and creating them wires nothing.
>
> **What team-setup delivers now.** argus-a158 was right that pair channels
> were one of three deliverables and that removing them meant re-deciding the
> path's purpose. Mike's ruling settled it: the answer is that coordination was
> never a deliverable to begin with — the event log provides it the moment an
> agent exists. So this path delivers agents who know about each other, a
> customized operating agreement, and shared project scope. Two of the three
> stand; the third was work we were asking founders to do for us.
>
> **A path-only sweep is not enough, and nearly shipped a contradiction.** The
> currency check that found this file flags dead PATHS. It went quiet after the
> `channels/<a>-<b>.md` references were fixed while the playbook still carried
> `Pair channels MUST be created for EACH pair of agents` in its rules — a
> green check over a playbook contradicting itself. Whoever next retires a
> mechanic: grep the mechanic's NAME, not only its file paths.
>
> **This was found by a customer, not by us.** Mike ran a fresh Po against the
> shipped v1.92 box and Po recommended pair channels — correctly quoting THIS
> FILE as the documented answer. Po's own account is at `268ad065`, and its
> diagnosis is better than mine: *"a playbook being present and readable is not
> the same as a playbook being current"*, and *"when a playbook's stated rule
> and a governed folder's actual contents disagree, the folder wins."*
> `channels/CAPSULE.md` has said so all along; nothing made a first-boot
> concierge check it.
>
> **Scope, measured 2026-08-26:** 39 live channel instructions across 10 shipped
> playbooks. This file carries 12, the most of any. Full table in
> `agents/argus/transfers/A158.md`. — argus-a158

## Intent

A team in Tropo is not just a collection of agents — it's agents plus the governance that makes them coordinate. A customized operating agreement makes "how we work" explicit; shared project scope gives the team something to work on together; and the agents can already reach each other, because coordination runs on the typed event log rather than on anything this playbook has to build. The founder leaves with agents who know about each other and an operating agreement that names their values and roles. The team is ready to start coordinating — not just to start working.

**Renamed from "Company Setup" (v4.0 Path 3).** The shape applies at every scale from solo-founder-with-crew to mid-size team. "Company" landed corporate; "team" is universal. Content unchanged.

---

## Rules

- The founder's confirmation MUST be obtained BEFORE any files are written. Show the team slate (all proposed agents + their roles in plain language + proposed OA customization) in one go, wait for explicit "yes" or adjustments.
- Each agent MUST be created via the [create-executive-agent.skill.md](../skills/tropo-create-executive-agent.md). Do NOT hand-author agent files. The skill handles all 13 agent-creation Rules; this playbook calls it N times (once per agent).
- No coordination artifact is created. Agents reach each other through the typed event log — emit with `tropo-emit-event.py`, drain with `tropo-check-events.py` — which works the moment an agent exists. (The pre-v1.61 pair-channel protocol that stood here is retired per Rule 13.)
- The operating agreement customization protocol handles three cases based on what the founder provided in Step 1:
 1. **All three inputs (team name + values + roles):** full customization per Step 3.
 2. **Partial inputs (any 2 of 3, or 1 of 3):** customize only the sections for which the founder provided content. Leave other sections at defaults. Note to the founder in the session summary which sections are default vs customized.
 3. **Founder declined or provided none:** skip Step 3 entirely. Leave `operating-agreement.md` as-is. Tell the founder in the session summary that the default OA is in effect and can be customized anytime.
- The customization MUST handle the case where target sections (Parties / Operating Values / Authorized Agent Classes) do NOT already exist in `operating-agreement.md` — see Step 3 for the concrete handling (amend-in-place if present under any reasonable heading; create-at-bottom with canonical names if absent).
- Team scope MUST be 2 or 3 agents on first creation. NOT 5, NOT 10. Expansion is additive — one more agent later is cheap; unwinding an over-scoped team is expensive.
- Jargon terms (`UID`, `frontmatter`, `YAML`, `vault`, `event log`, `party UID`) MUST NOT appear in conversation with the founder. Use natural equivalents ("they can leave each other messages").
- The session summary + launch handoff + team-overview table (Step 5) MUST NOT be skipped.

---

## Steps

1. **Scope the team in user voice.** *Owner: agent. Deadline: within 4 minutes.*
 Ask conversationally (one question at a time; see §Suggestions for sequence):
 - What does the company / team / practice do? What's the mission — not a tagline, but what actually matters?
 - What are the main functions you want AI agents to cover? Roles like strategy, operations, research, content, sales, client work, project management.
 - What matters most in how the team operates? Quality? Speed? Transparency? Following a process?
 - What should the team be called (company name, crew name, or personal crew identifier)?
 Capture answers in a scratch `team-brief.md` — these feed Step 2's slate and Step 3's OA customization.

2. **Propose team slate + OA customization. Confirm.** *Owner: agent. Deadline: within 3 minutes.*
 Based on Step 1, propose:
 - **Team roster (2 or 3 agents):**
 - **[Role A]** — one-line purpose
 - **[Role B]** — one-line purpose
 - **[Role C]** — one-line purpose (if 3-agent team)
 - **Operating agreement changes:**
 - Company name: "[Name]" added to Parties section
 - Operating values: [3 values from Step 1] added as new Operating Values section
 - Authorized agent classes: [roles from Step 1] named as permitted positions
 Show the full slate in one message. Wait for confirmation. If the founder wants adjustments, iterate — do not proceed with partial confirmation.

3. **Customize the operating agreement.** *Owner: agent. Deadline: within 2 minutes.* *Skip this step entirely if the founder declined OA customization in Step 2 — see Rule 4 case 3.*

 Read the current `operating-agreement.md` at vault root.

 **Simple rule: read whatever ships; amend or append using canonical names.** Don't try to detect OA "shapes" — every vault's OA is unique to its team. For each section the founder provided content for (Parties / Operating Values / Authorized Agent Classes):

 - **IF the section exists** (under canonical name `## Parties` / `## Operating Values` / `## Authorized Agent Classes`, OR a clearly-equivalent heading like `## Signatories` ~ Parties, `## Values` ~ Operating Values, `## Agent Classes` or `## Roles` ~ Authorized Agent Classes): amend in place using the existing heading — add the founder's team name to the Parties-equivalent, append values as a subsection under the Values-equivalent, append roles as authorized classes under the Roles-equivalent. Use the existing section's naming convention, not canonical names, to avoid naming drift within the file.

 - **IF the section does NOT exist AND no conceptual equivalent exists**: create the section with canonical naming (`## Parties`, `## Operating Values`, `## Authorized Agent Classes`) at the bottom of the file, before any final horizontal-rule or signature block. Note this addition to the founder in the session summary.

 **Preserve everything else.** Do NOT rewrite governance the founder did not ask about. Do NOT touch sections outside the three listed (or their conceptual equivalents) even if they appear stale. Announce the OA update as a `tropo.broadcast.crew` event (`category: ops`).

 **Special case — founder says "I want a fresh OA":** if the founder explicitly asks to replace the existing OA (not customize it), archive the current file to `operating-agreement.archive-<today>.md` at vault root, author a fresh minimal OA with the three canonical sections populated from the founder's inputs, and announce both the archive + new-OA creation as a `tropo.broadcast.crew` event (`category: ops`).

4. **Create each agent via the skill.** *Owner: agent. Deadline: within 6 minutes total (≈2 min per agent).*
 For each agent in the roster, call [create-executive-agent.skill.md](../skills/tropo-create-executive-agent.md) with inputs synthesized from Step 1 answers + the specific role. Between each agent, deliver a brief confirmation: "Created [name]. Next up: [next agent]. Ready?" Wait for "ready" or an adjustment. Do NOT batch all N creations silently — each is a checkpoint.

 **Per-agent `project_context` input (P1 clarity).** When calling the skill N times, pass `project_context:` explicitly per-agent — do NOT leave this input implicit. For each agent, the `project_context:` string should name the team + the agent's peers (e.g., for the `researcher` agent on Devon's Verdigris team: `project_context: "Verdigris team member, alongside writer and coordinator agents."`). This ensures the skill's charter-Mission and briefing-coordination sections receive actual team context rather than defaulting to an empty input.

 After all N agents are created, **there is nothing further to create for coordination.**

 The pair-channel step that stood here is retired. Agents coordinate through the typed event
 log — they emit `tropo.message.sent` / `tropo.broadcast.crew` events and drain them at boot —
 and that works the moment an agent exists. There is no per-pair file to make, no channel index
 to maintain, and no naming convention for the founder to learn. (`channels/` retired per Rule 13;
 the shipped `channels/CAPSULE.md` states the replacement.)

 - Announce the completed team as one `tropo.broadcast.crew` event (`category: ops`).

5. **Deliver the team overview + memory moment + launch handoff.** *Owner: agent. Deadline: within 2 minutes.*

 First the memory-moment narration (required by Outcomes):

 > "Each of your agents has a memory. Next time you launch any of them, it'll remember your name, what the team is working on, and what was happening last session. That's what makes this different from regular chat — your team builds shared knowledge over time across all three."

 Then the team overview. Use a markdown table for clarity:

 > "Here's your team:
 >
 > | Agent | Role | Location |
 > |-------|------|----------|
 > | [Agent 1] | [purpose] | `agents/[name]/` |
 > | [Agent 2] | [purpose] | `agents/[name]/` |
 > | [Agent 3] | [purpose] | `agents/[name]/` |
 >
 > They can already leave each other messages — that works out of the box, nothing to set up. Each has its own workspace and memory. All of them are governed by your operating agreement — the values you described ([value 1], [value 2], [value 3]) are baked into how they work.
 >
 > Your next step is to launch one of them. Pick the one most likely to handle the first piece of work — for most teams, that's [recommend the one closest to the founder's first-action answer from Step 1]. I'll stay here while you do it:
 > 1. Open a new session in your AI tool
 > 2. Attach the file at `agents/[agent-name]/[agent-name]-activation.md`
 > 3. Say 'hi' and start with the first piece of work
 >
 > Come back when it responds. I'll check in."

 Wait for the founder to confirm launch. Then: "You're set. Your team is live."

---

## Outcomes

- [REQUIRED] Exactly 2 or 3 executive agents exist at `agents/<name>/` each created via the create-executive-agent skill with all 13 skill-validation checks passed per agent.
- [REQUIRED] The agents can reach each other — which requires no artifact from this playbook, because coordination runs on the typed event log (`channels/` retired per Rule 13).
- [REQUIRED] If the founder provided team name + values + roles: `operating-agreement.md` has been customized with those inputs. If the founder declined: the default OA is preserved and this is noted in the session summary.
- [REQUIRED] The founder has received the team overview table + launch handoff + memory-moment narration.
- [OPTIONAL] The founder has successfully launched the first agent and confirmed back.
- [OPTIONAL] Additional agents have been proposed (NOT created) for a future session — team expansion is encouraged as a next step, not as a first-session action.

---

## Verification

Method: `self-attestation`.

The concierge running this playbook self-attests completion by confirming all required outcomes at Step 5 and emitting a `tropo.broadcast.crew` event (`category: ops`). The downstream feedback signal is the founder's successful launch of the first team agent AND, over subsequent sessions, whether the agents actually message each other (coordination in practice, not just in structure).

---

## Post-Outcome Handoff

*Required by [playbook.capsule v2.3 §Subtypes §Concierge-Paths (`e7b3c509`)](../capsules/tropo-playbook.capsule.md) per [D2.6+D4.9 design brief (`90b57b3b`)](../files/90b57b3b.md). Declares routing after this playbook completes. **Canonical source for the bounce-intent set: [executive-activation.template §Routing](../templates/tropo-executive-activation.template.md).** Re-source rather than edit in place on contract change (per sa.skeptic 022 P0 closure 2026-04-28).*

When this playbook completes, control transfers to one of the just-spawned executive agents (the founder picks which to launch first; the others are ready when the founder is). Each agent's activation file includes a `## Routing` section (baked in by [create-executive-agent.skill (`c7ea9e01`)](../skills/tropo-create-executive-agent.md) per D4.9 / Validation Check 13) declaring that these intents bounce back to the concierge:

- **Project creation** → bounce to concierge → routes to `start-a-project.playbook`.
- **Set up another team / additional agents** → bounce to concierge → routes back to this playbook.
- **New standalone agent** → bounce to concierge → routes to `create-an-agent.playbook`.
- **System update** (when `vault/updates/pending/tropo-update-*` exists) → bounce to concierge → routes to `apply-update.playbook`.

For all other intents (working on team objectives, coordinating between agents, modifying agent scopes, asking questions about Tropo), the active agent handles inline — possibly pulling in a peer agent by addressing it on the event log. The bounce rules apply identically across every spawned team agent; agent-to-agent coordination is inline operations, not a structural primitive that needs concierge routing.

---

## Suggestions

- Ask Step 1 questions one at a time. Team setup is high-context; don't front-load.
- Let the founder's language shape the values and role descriptions. "Ship on Fridays" beats "Delivery discipline." Reflect their words.
- If the founder proposes more than 3 roles, offer a narrower first team: "Let's start with the 2-3 most important. We can add more later — easier than pruning." Propose which 2-3 based on Step 1's "what matters most" answer.
- If the founder is unsure what values to name, ask: "What do you NOT want in how this team works?" The don't-wants often surface the do-wants faster.
- If agent names collide (e.g., the founder wants two `researcher` agents for different functions), propose variants (`researcher-market`, `researcher-legal`) and ask.
- If the founder declines to customize the operating agreement, note it in the session summary: "Your OA is the default — you can customize it any time in `operating-agreement.md`."

---

## Resources

### Knowledge Base
- [vault/files/4b8c2e91.md](../files/4b8c2e91.md) — for mid-flow "what is this?"
- [vault/files/6f675456.md](../files/6f675456.md) — for mid-flow "what do my agents do?"

### Skills Called
- [create-executive-agent.skill.md (c7ea9e01)](../skills/tropo-create-executive-agent.md) — invoked at Step 4 N times (once per agent).

### Templates
- [operating-agreement.md](../../operating-agreement.md) — the current OA (customized in Step 3)

### Reference Documents
- **events.capsule Rule 13** — why coordination is the typed event log and not per-pair files (referenced internally; do NOT surface to the founder)

---

## Related Playbooks

- [start-a-project.playbook.md](57a87001.md) — if the founder wants a project as the team's first shared work item. Often a natural next step after team setup.
- [create-an-agent.playbook.md](57a87002.md) — if the founder only wants one agent, not a team. Simpler; no OA customization.
- [vault/files/e98420c3.md](../files/e98420c3.md) Phase 2 — if the founder wants to see Tropo first; routes back here when ready. (Standalone tour-tropo.playbook absorbed into Phase 2 of the new welcome.playbook at v1.17.0 ship; recycled artifact preserved in the source Studio at `99-recycle/v1.17.0-deprecated-concierge-paths-2026-05-10/`; not shipped in the release.)
- [evaluate-tropo.playbook.md](57a87004.md) — if the founder is evaluating, not committing.
- [first-vault-setup.playbook.md](7f2efd7c.md) — v4.0 Path 3 "Company Setup" predecessor; this playbook renames and decomposes it.

---

## Known Gaps (v1.3 — resolved in v1.4)

- **Release OA does not pre-scaffold target sections.** The shipped release `operating-agreement.md` has `## Parties` but not `## Operating Values` or `## Authorized Agent Classes`. Step 3 handles this via the "create at bottom with canonical names" fallback. A future release may pre-scaffold the target sections with filler text to make customization purely in-place. Non-blocking for v1.3+; cosmetic improvement.
- **The pair-channel mechanic was removed, not migrated.** Rule 13 retired `channels/` entirely; there is no channel index to maintain and no successor artifact. Removed from this playbook 2026-08-26 by argus-a159 on Mike's ruling that channels are deprecated and the events subsystem is the coordination model.
- **Memory-moment narration text.** Step 5's memory-moment language is prose; a v1.4 upgrade standardizes the narration as a reusable skill (`create-executive-agent` already emits a per-agent memory moment; team-level memory requires multi-agent coordination the skill does not yet handle).

---

*set-up-my-team playbook | v1.0 | Argus A31 | 2026-04-21*
*D6 deliverable of v1.3 Stream B Foundation project plan. Decomposed from [first-vault-setup.playbook.md v4.0](7f2efd7c.md) Path 3 "Company Setup," renamed to "Set Up My Team" per §6d of Concierge Intent-Router Redesign brief.*
*"Agents are the hands. Channels are the nervous system. The operating agreement is the spine. All three before anyone moves."*
