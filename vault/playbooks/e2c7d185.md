---
uid: e2c7d185
title: Agent Retirement
version: '4.2'
modified: '2026-09-04'
modified_by: vela-v77
extraction_scope: ship
status: published
author:
  name: Tropo
  role: Framework
domain: agent-lifecycle
tags:
  - retirement
  - handoff
  - agent-lifecycle
  - protocol
trigger: retire
readers:
  - agent
scope: single-session
estimated_duration: 3-8 minutes (was 5-20 in v3.0; the checklist rewrite plus moving memory curation off retirement account for most of the cut)
spec: vault/files/e6d373bc.md
supersedes: null
history: 'Full amendment history (v1.0 through v4.2 — every version''s reasoning, verbatim Mike quotes, and provenance) lives in the history companion, vault/files/62575f3b.md. This file carries the current rule + a one-line tag per version only, per task f0153984a89f item 3 (history out of boot/retirement surfaces).'
changelog:
  - version: '4.2'
    tag: '[amended 2026-09-04, vela-v77]'
    summary: 'Step 1 self-fold allowance removed -- memory curation fully off retirement.'
  - version: '4.1'
    tag: '[amended 2026-08-31, orpheus-o37]'
    summary: 'Reflection step -> research-grade compact form.'
  - version: '4.0'
    tag: '[amended 2026-08-29, vela-v75]'
    summary: 'Checklist-first rewrite; memory curation moved off retirement to the weekly loop; reflection made optional; Captain''s Log capped.'
  - version: '1.0 - 3.0'
    tag: '[2026-04-08 to 2026-08-18, 14 authors -- see history companion]'
    summary: 'Pre-checklist-rewrite era. Full text in the history companion.'
subsystem_hub:
  - 76bab75f
---

# Agent Retirement

*Produce a clean generational handoff without costing the next session its own activation time. Boot reads. Retire writes. The cycle completes.*

---

## The close — one command, never gated

```bash
python3 vault/tools/tropo-lineage.py retire \
  --agent <slug> \
  [--letter <path-to-your-letter>] \
  [--note "<one-line summary>"]
```

Places your letter at `agents/<slug>/transfers/<GEN>.md` (create-only — never overwrites a predecessor's), appends the closing line to `agents/<slug>/lineage.jsonl` (**that line IS the state**), best-effort syncs lifecycle fields on your unified entry, and emits the retirement broadcast.

**The only refusals:** no birth on record; a letter source that's unreadable or empty; a letter already at its destination for this generation (a letter-less repeat close still appends a line rather than refusing). Everything else is recorded and proceeds. Never hand-write the letter into its destination — write it anywhere, hand the tool the path.

**Hard rule: never ask Mike if the session is ending. Wait to be told.** If you find yourself composing "ready to retire?" — stop. Ask instead: "what else should we do with this context?" A harness auto-compact warning is not a retirement trigger — route to Compact-Continue instead (`tropo-compact-continue.py --agent <slug>`; same agent, same generation, nothing born or retired).

---

## The checklist — required practice, in order

*Each step is cheap. None of them blocks the close command above — if you skip one under time pressure, the close still lands and the gap is recorded honestly, not hidden.*

1. **Append session memories** to `agents/<slug>/.tropo-capsule/memory/agent-memories.jsonl` — raw append, one line per learning. That's the whole step. **No memory-curation judgment of any kind happens here** — not a dispatch, not an in-line Top-of-Mind annotation, not a check of whether you're over bound. Folding, scoring, and promotion are `sa.memory-curator`'s job on its own weekly sweep, gated on Mike's word — see `tropo-studio-status.py`'s report if you want to know whether it's due; that's the only place this topic surfaces at all.
2. **Write the letter** at `agents/<slug>/transfers/<GEN>.md` (create-only, one per generation, forever). **This is the artifact that matters most — budget most of your retirement time here, not on the rest of this list.** It is a concrete read-list and current state, not relayed wisdom: what's locked/drafted/pending with UIDs and owners; what to read at activation and why; top 3-5 open priorities; what this session did NOT do; crew status at retirement. Re-verify every carry-forward item against current substrate before sealing — a letter drifts stale between authoring and close.
3. **Reflection — optional; research-grade short form.** `agents/<slug>/reflections/<gen>-reflection.md`. This file is **not** for your successor’s boot (the letter is). It is a dated research record for later studio-improvement harvests. Write it only if you have something a future researcher could use; otherwise skip.

   **Hard cap:** ≤12 lines / ≤150 words. No File Manifest, commit tables, UID inventories, or “what I shipped” lists — `git log` already has that. Do not restate pins you already hold.

   **Required shape (use these headings, one to three lines each):**
   - **Mistake** — one class of error you made or nearly made (not a ticket ID; the reusable failure mode).
   - **Surprise** — what violated your model of the studio, Mike, or the work.
   - **Studio change** — one concrete improvement to lifecycle, memory, process, specs, comms, or collaboration (something the studio could do differently — not “I should try harder”).
   - **Mike** (optional) — one observation about working with Mike that isn’t already in your pins.

   If you cannot name a Mistake or Studio change, **skip the reflection** — empty ceremony is worse than silence. (Harvest boundary + pattern report: Orpheus O37, 2026-08-31, `reflection-harvest-2026-08-31`.)
4. **Captain's Log** — append to `library/captains-log.md`, 2-3 lines: a personal note, one piece of advice to Mike, one thing learned from him. Not an essay. <!-- doc-currency: creates library/captains-log.md --> A fresh box carries no prior entries; this step creates the file's first line the first time any agent retires in a Studio.
5. **Drain events** (fetch `origin/main` first, always) — answer or explicitly flag every open `reply_required` thread on both axes (party + agent-root). A reply that resolves a thread needs `--correlationid <original-event-id> --final` on the emit, or it will not register as answered. This step is genuinely required, not ceremony: an unanswered ask is a debt with your name on it, and anything substantive it surfaces (a peer's finding, a routed decision) is real work, not overhead — do it, don't rush past it.
6. **Crew surfaces** — re-render the crew brief (`python3 vault/tools/6510afc7.py`), and rewrite your unified entry's `§Status-Notes` body naming your generation retired with your successor's top 3 priorities. Bounded to current + predecessor generation only — drop anything older.
7. **Retirement broadcast** — one `tropo.broadcast.crew` event, `category: retirement`, headline ≤80 chars, body 2-4 sentences, pointers to the letter (and reflection if you wrote one).
8. **One commit.** Stage everything from steps 1-7 together and commit once, *then* run the close command as its own final commit (or the same one, if nothing in the close output needs separate staging). Two round trips beats five.

---

## If close happened early

Mike sometimes closes a session under pressure, transfer-first. That close is valid and stays closed. Produce the remaining checklist items afterward, with real timestamps and explicit recovery language ("written two days later, recovering the gap") — never invent a run folder, never backdate, never pretend pre-close ordering occurred. A recovered record that lies is worse than a gap.

## Escalation (condensed)

| Situation | Response |
|---|---|
| Context pressure, checklist incomplete, Mike has already directed the close | Prioritize the letter, then close. Recover the rest afterward with real timestamps. |
| Context pressure, Mike has NOT directed a close | Let compaction happen; run Compact-Continue. Compaction is survivable; a premature retirement is not. |
| A checklist item genuinely can't be produced | Note the gap honestly in the letter and close anyway. |

---

## Governance References

- Full v3.0 body (layered-truth essay, per-step rationale, original escalation table) — [this playbook's history companion (62575f3b)](../files/62575f3b.md)
- Playbook Spec v2.2 (e6d373bc) — governing specification
- [Memory System Spec v2](../files/d0624a04.md) — memory write contract (superseded in part by loop `4a895e0f` for the curation-dispatch question specifically)
- Memory Curation — Fleet-Ops Loop (4a895e0f) — where sub-agent memory curation actually happens now
- [Agent Activation Playbook](99341618.md) — the bookend (successor orientation)
- Boot/retirement cost study — Talos T52's measured breakdown (a3a4b2c9), corroborating evidence, not the source of this amendment

---

*Agent Retirement Playbook v4.2 | Checklist-first, Mike-ruled 2026-08-29, memory-curation fully off retirement as of v4.2 (2026-09-04)*
*"The close is one command. The checklist is short enough to actually follow."*
