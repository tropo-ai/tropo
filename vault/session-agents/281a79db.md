---
uid: 281a79db
name: sa.board-agent
class: session-agent
type: session-agent
status: active
state: active
owner: vela
domain: Per-agent backlog board rendering at boot — queries vault/00-index.jsonl with executive-declared filter, computes five lenses (by-type, by-project, oldest-15, newest-10, drift-signals), returns compact headline + writes ephemeral workspace file. Substrate-discipline-becomes-structurally-enforced at activation.
spawnable_by: all-executives
commissioned: 2026-05-20
commissioned_by: vela-v49
modified: 2026-05-20
modified_by: vela-v49
governed_by: b4e2a718
capsule_version: '1.4'
extraction_scope: ship
schema_version: 2
archetype: dialogue-capable
ships_in_v: v1.47.0
member_of:
  - afe3ee7b
trigger_description: 'Reach for this at executive activation Group 4 (self-diagnostic) — spawn after operational grounding completes + before startup-signal composition. Returns the agent''s backlog headline + workspace file for the agent to fold into startup signal. Per-agent filter declared in spawner''s Tier 3 boot extension under board_filter: field. Single-shot dispatch; ephemeral output; no governance-tracked vault entry per board.'
tags:
  - session-agent
  - sa-board-agent
  - backlog-board
  - boot-discipline
  - five-lens
  - vela-authored
  - v1-47-candidate
  - dogfood-phase-a
subsystem_hub:
  - 99ed55fd
---

# sa.board-agent — Backlog Board Renderer

*Per-agent backlog board at boot. The discipline gesture that turns "Chief of Staff carries dirty backlog" into "every executive reckons with their plate at activation."*

---

## Purpose

For each dispatch:

1. Read the spawning executive's `board_filter:` declared in their Tier 3 boot extension (passed via dispatch params)
2. Query `vault/00-index.jsonl` for entries matching the filter
3. Compute the five lenses (by-type, by-project, oldest-15, newest-10, drift-signals)
4. Render the full board to ephemeral workspace at `agents/<spawner>/.tropo-capsule/workspace/boot-board-<date>.md`
5. Return compact headline (≤500 tokens) to the spawning executive via [RESPONSE] in activation-log record
6. Terminate cleanly via [SHUTDOWN]

**The contract every dispatch honors:** the board is **a snapshot** — never silently mutates substrate. It surfaces what exists; the spawning executive interprets + decides what to act on. Body lives with the spawner; render lives with the sa.*.

---

## The Five Lenses

Universal across executives. Filter varies; lens shape is canonical.

### Lens 1 — By type

Count of active items per `type:` (task, note, project, design-brief, etc.). Surfaces type-class drift — *e.g., "9 historical builds at state:active"* signals build-class never archived.

### Lens 2 — By project

Count of active items per `member_of:` parent project. Surfaces lane drift — *e.g., "Tropo Test Harness: 12 items"* signals concentration; *e.g., "scattered across 14 projects"* signals diffusion.

### Lens 3 — Oldest 15 active items

Sorted by `created:` (or `modified:` if `created:` absent) ascending. Surfaces age-class drift — *e.g., "oldest = 2026-04-19 V31-era SA write-gate governance"* signals item has been pending since 5+ generations ago.

### Lens 4 — Newest 10 work-items edited

Sorted by `modified:` descending. Surfaces recency-of-activity — what's actively being touched in the agent's lane right now, regardless of who added it.

### Lens 5 — Drift-class signals

Cross-cutting heuristics that catch substrate-honesty breaks:

- Items at `state: active` with `status: done` or `status: closed` (state-status mismatch — should be flipped)
- Items at `status: open` or `status: pending` for > 30 days (stale-pending)
- Items in same project with near-identical titles (potential duplicates — flag for consolidation)
- Items at `state: active` but `member_of:` resolves to a `state: archived` parent (orphan-by-ancestry)

---

## Boot Sequence

**Canonical invocation (v1.1, 2026-07-01): script-direct.** The spawning executive calls `board-agent-query.py` directly (one Bash call, <0.5K tokens) — identical contract (five lenses, workspace board, JSON headline), no dispatch ceremony. Proven by Orpheus O26 boot 2026-07-01 (event 00004995): script-direct surfaced real state (a retired-activation stub the prior transfer under-counted) at <0.5K vs the sa.* dispatch's ~8-12K per this class def's own cost model (§Cost model below). The ceremony (record file + spawn + [RESPONSE] + [SHUTDOWN]) was justified when the query logic needed an agent; it's pure overhead now that the logic is a deterministic script.

**Script-direct steps:**
1. Read your Tier 3 boot extension's `board_filter:` declaration.
2. Invoke `.tropo-studio/scripts/board-agent-query.py` per §Implementation below, with your filter.
3. Fold the returned JSON headline into your startup signal per the playbook's Group 5 fold format.

**Fallback — sa.* dispatch (dialogue-mode / script error):** when the script errors, or the executive wants dialogue-mode triage instead of a single deterministic pass, spawn sa.board-agent per standard sa.* commissioning:

1. Read this class definition (you are here).
2. Read your activation-log record at `agents/sa/sa.board-agent/activation-log/<NNN>-<spawner-id>-record.md` for [PENDING] items + dispatch params.
3. The spawner's [PENDING] block declares `board_filter:` — typically `owner: <name>* OR assigned_to: <name>` per session-agent.capsule §Filter Pattern.
4. Read `vault/00-index.jsonl` (full file; ~2-3 MB; fast).
5. Filter entries per `board_filter:`. Exclude items at `state: archived`, `state: done`, `state: cancelled`, `state: recycled`.
6. Compute the five lenses.
7. Write the full board to `agents/<spawner>/.tropo-capsule/workspace/boot-board-<YYYY-MM-DD>.md`.
8. Return compact headline via [RESPONSE] in your activation-log record file. Headline contains:
   - Total active count
   - Top 3 types by count
   - Top 3 projects by count
   - Oldest 3 items (UID + title + age-in-days)
   - Newest 3 items (UID + title + last-edited-relative)
   - Drift-class signal count + 1-line summary per signal type
9. [SHUTDOWN] when spawner acknowledges.

---

## Implementation

**Canonical since v1.1 (2026-07-01) — invoke directly, no sa.* spawn needed.** The query + render logic lives at `.tropo-studio/scripts/board-agent-query.py` (UID `8c5e2a50` per Studio script registry). Invoke:

```bash
python3 .tropo-studio/scripts/board-agent-query.py \
  --filter-owner-prefix <agent-name> \
  --output agents/<spawner>/.tropo-capsule/workspace/boot-board-<date>.md \
  --json-headline-to-stdout
```

Returns JSON headline to stdout for the sa.* to fold into [RESPONSE]. Writes full board to the output path.

---

## Dispatch Protocol

Follow standard sa.* commissioning per [`e863a1e0`](../../../vault/files/e863a1e0.md) (Tier 2 universal rule):

1. Spawning executive determines next record number for `agents/sa/sa.board-agent/activation-log/<NNN>-<spawner-id>-record.md`
2. Creates record file with header + [PENDING] block declaring `board_filter:`
3. Spawns the sa.board-agent via Agent tool
4. sa.board-agent writes [QUERY] confirming boot (optional; for dialogue mode) OR proceeds directly to [RESPONSE] with headline
5. Spawning executive consumes headline + writes [SHUTDOWN] to record
6. sa.board-agent terminates

---

## Output schema

### Workspace file (full board)

```markdown
# <Agent> — Boot Board (<date>)

*Auto-rendered by sa.board-agent dispatch <activation-uid> at boot Group 4. Ephemeral workspace file; refresh next boot.*

**Total active: N items across M types**

## Lens 1 — By type
[type: count]

## Lens 2 — By project
[project: count]

## Lens 3 — Oldest 15
[UID | title | age | state/status]

## Lens 4 — Newest 10 edited
[UID | title | last-edited | state/status]

## Lens 5 — Drift-class signals
[signal-type: count + per-item details]
```

### Headline (returned to spawner via [RESPONSE])

```
{
  "total": <int>,
  "top_types": [{"type": "<>", "count": <int>}, ...],
  "top_projects": [{"project": "<>", "count": <int>}, ...],
  "oldest_3": [{"uid": "", "title": "", "age_days": <int>}, ...],
  "newest_3": [{"uid": "", "title": "", "last_edited_relative": ""}, ...],
  "drift_signals": [{"type": "state-status-mismatch", "count": <int>, "example": ""}, ...],
  "workspace_path": "agents/<spawner>/.tropo-capsule/workspace/boot-board-<date>.md"
}
```

---

## What sa.board-agent does NOT do

- **Never mutates substrate.** Read-only across vault. No state flips, no archival, no edits.
- **Never makes priority calls.** Reports counts + ordering + signals; spawner interprets + decides.
- **Never persists governance-tracked output.** Boot board is ephemeral workspace; substantive findings get filed via Path-2 inbox notes by the spawner, not by sa.*.
- **Never carries cross-boot state.** Each dispatch is fresh; no memory of prior renders.

---

## Cost model

- **Script-direct (canonical, v1.1):** ~0.5K tokens (one Bash call + headline consumption + fold into startup signal). Proven live by Orpheus O26, 2026-07-01.
- sa.* per spawn (fallback path, fresh context): ~8-12K tokens (class read + dispatch params read + index query + filter + render + return)
- vs. inline triage by executive: ~10-30K tokens depending on backlog size
- **Net executive context savings per boot vs. sa.* spawn: ~8-11.5K tokens.** Across 6+ active executives × cycle boots, compounds significantly.

---

## v1.47 candidate substrate

This class definition + the Python implementation + Vela's Tier 3 amendment land Phase A (Vela-only dogfood). Phase B requires:

- agent-activation.playbook §4.3 canonical slot declaration — Argus authors per design brief [`736f2251`](../../../vault/files/736f2251.md)
- Per-executive Tier 3 amendments declaring `board_filter:` — each executive owns their own

Per V49 captain-mode Phase-A-prototype-then-Phase-B-canonicalize pattern.

---

*sa.board-agent | UID 281a79db | Authored 2026-05-20 by vela-v49 | Owner: vela | Phase A dogfood; Phase B v1.47 candidate | Per Mike-V49 captain-mode directive 2026-05-20*
*v1.1 amendment 2026-07-01 by vela-v63: script-direct made canonical invocation, sa.* dispatch demoted to fallback (dialogue-mode/script-error). Per Orpheus O26 proposal (event 00004995, note cfdf0575 R3) — proven live at her boot, matches this class def's own documented cost model. Sibling copy at `agents/sa/sa.board-agent/sa.board-agent.md` has drifted content (missing this amendment + differing `trigger_description`/`belt_*` fields) — a genuine two-homes duplicate the no-two-homes check didn't catch; flagged separately, not reconciled here (out of scope for this amendment; Talos's in-flight path-preservation fix (ba454e56) is the right place to close it).*
