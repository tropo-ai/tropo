---
uid: 547e0cac
title: close-loose-ends — test-pipeline step 6
type: pipeline
subtype: workflow-node
name: close-loose-ends
version: '1.0'
author: vela-v66
owner: vela
status: active
state: active
role: step
created: 2026-07-14
created_by: vela-v66
modified: 2026-07-31
modified_by: talos-t37
schema_version: 2
extraction_scope: ship
governed_by: e4c8a6b2
children: []
step_owner_role: vela
step_verifier_role: same-as-executor
verification_class: true
verification_command: >-
  python3 -c "import sys,yaml,pathlib;
  a=yaml.safe_load(pathlib.Path('vault/files/{activation}.md').read_text().split('---',2)[1]);
  r=a.get('activation_root_project');
  f=yaml.safe_load(pathlib.Path('vault/files',r+'.md').read_text().split('---',2)[1]) if r else {};
  sys.exit(0 if f.get('state')=='archived' else 1)"
depends_on_steps:
  - 047c147c
exit_criteria:
  - activation_root_project.state == archived
trust_level: standard
next_steps: []
relationships:
  - rel: member_of
    uid: 92133de1
member_of:
  - 92133de1
tags:
  - pipeline
  - workflow-node
  - test-pipeline
  - step
  - close-loose-ends
  - terminal-step
  - housekeeping
  - archival
  - mike-directed
---

# close-loose-ends — test-pipeline step 6

<!-- nav-block:start -->
**📍 Vault Path:** [tropo-work](b8e5f3a2.md) → [test-pipeline](da3f50dc.md) → [verify-and-close — test-pipeline stage](92133de1.md) → **close-loose-ends — test-pipeline step 6**
<!-- nav-block:end -->

## Purpose

Same pattern and same Mike-directed housekeeping goal (2026-07-14) as the dev-pipeline sibling, [close-loose-ends — dev-pipeline step 8 (8a476130)](8a476130.md) — see that entry for the full rationale + evidence. This is the test-pipeline's own terminal-adjacent sweep, since test-pipeline activations mint their own Activation Root scaffolding project exactly like dev-pipeline does (12 of the one-time catch-up sweep's 23 unswept pipeline projects were `test-pipeline — Activation Root` entries).

`close-activation (047c147c)` is already Vela-owned substrate, so this extension needs no cross-owner authorization — unlike the dev-pipeline sibling, which required notifying Argus.

## Structure

Leaf node — `role: step`, `children: []`, and terminal in its stage (`next_steps: []`, so nothing follows it).

```
test-pipeline (da3f50dc)
└── verify-and-close — stage (92133de1)
    ├── validate-coverage — step 4 (a69fcab3)
    ├── close-activation — step 5 (047c147c)
    └── close-loose-ends — step 6 (547e0cac)   ← this node, terminal
```

Execution reaches this node by the flow edge rather than the tree: `close-activation`'s `next_steps:` carries `[547e0cac]` per the amendment below, and this node declares `member_of: 92133de1` upward. The stage's own `children:` array still lists only steps 4 and 5, so the downward half of that membership is unwritten — a runner following `next_steps:` arrives here, while anything that walks the stage's children does not see this node.

## Nodes

No child WorkflowNodes. The child table the capsule asks for is empty by construction, not unwritten: `role: step` with `children: []` is a leaf. Upward it belongs to the verify-and-close stage ([92133de1](92133de1.md)); its only sibling edge is inbound, from `close-activation — step 5` ([047c147c](047c147c.md)).

## Flow Rules

**Activates when:** step 5 `close-activation` ([047c147c](047c147c.md)) reaches status:done — single-input handoff (`depends_on_steps: [047c147c]`).

**Completes when:** the declared exit criterion passes — `activation_root_project.state == archived`, evaluated by this node's `verification_command` against the activation's `activation_root_project`.

**This is now the terminal step.** No `next_steps`. Pipeline-run completes after this step verifies.

## Cold-Boot Walk-Through

Test-spec activation closed and back-fed to the triggering dev-pipeline cycle (step 5). Before this pipeline-run terminates, the executor sweeps up everything that fed into it:

1. **Find the loose ends.** Every task, note, and design-brief that fed into this test-spec's work — linked via `member_of:`/`refs:` to this activation's work-items.
2. **Close what is genuinely done.** For each: if the underlying work is actually complete, close it out per its own capsule's terminal transition. **Do not force-close anything still legitimately open or ambiguous — leave it and note it in the closing summary.**
3. **Archive what is closed.** Every task/note/design-brief closed in step 2 (or already closed but still `state: active`) gets archived via `python3 vault/tools/tropo-archive.py <uid> --reason "cycle <this-activation-uid> closed, swept at close-loose-ends"`.
4. **Archive the Activation Root project.** This test-pipeline run's own scaffolding project gets archived — `--reason "pipeline-run <activation-uid> completed"`.
5. **Record the sweep.** A brief summary goes into this pipeline-run's own closing substrate.

## Amendment to close-activation

[close-activation (047c147c)](047c147c.md)'s `next_steps:` is amended from `[]` to `[547e0cac]` in the same gesture that authored this step. Both entries are Vela-owned; no cross-agent authorization needed.

## Known Enforcement Gaps

The declared exit criterion covers action 4 only — that the Activation Root project reached `state: archived`. Actions 1, 2, 3 and 5 (find the loose ends, close what is genuinely done, archive it, record the sweep) are executor judgment with no machine check, so a run can verify green having archived the root and swept nothing.

Step 2's "do not force-close anything still legitimately open or ambiguous" caveat is likewise unenforceable — the closing summary in action 5 is the only record that a loose end was deliberately left open, and nothing verifies that summary exists.

## Changelog

| Version | Date | Change | Author |
|---------|------|--------|--------|
| 1.0 | 2026-07-14 | Initial authoring. Test-pipeline's own terminal close-out sweep, Mike-directed housekeeping; `close-activation` (047c147c) amended to chain here. | vela-v66 |

---

*close-loose-ends | step WorkflowNode | verify-and-close stage (92133de1) | test-pipeline (da3f50dc) | author vela-v66, 2026-07-14, Mike-directed housekeeping*
