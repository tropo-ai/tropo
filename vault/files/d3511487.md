---
uid: d3511487
title: author-test-substrate — test-pipeline step 2
type: pipeline
subtype: workflow-node
name: author-test-substrate
version: '1.0'
author: vela-v51
owner: vela
status: active
state: active
role: step
created: 2026-05-23
created_by: vela-v51
modified: 2026-05-23
modified_by: vela-v51
schema_version: 2
extraction_scope: ship
governed_by: e4c8a6b2
children: []
step_owner_role: vela
step_verifier_role: same-as-executor
verification_class: false
depends_on_steps:
  - 846a0909
exit_criteria:
  - all test_substrate_path entries in test-spec exist on disk OR are explicitly marked new-file-authored in this step
  - 'machine_executable_script entries: real .py/.sh/.ts files at named paths; executable; exit non-zero on behavior break'
  - 'structural_check entries: validator extension authored in tropo-validate.py (function named per plan)'
  - 'deterministic_assertion entries: DSL expression parses against pipeline.capsule v3.0 §6 grammar'
  - 'agentic_review entries: dispatch_target sa.* class registered + commissioning record path prepared per [e863a1e0]'
  - harness_framework_changes_required scope-expansion landed (Vela substrate + Talos engineering + Argus capsule/spec amendments) if flagged at step 1
trust_level: auto-with-verification
next_steps:
  - 05d9ecc5
relationships:
  - rel: member_of
    uid: 2e097029
member_of:
  - 2e097029
tags:
  - pipeline
  - workflow-node
  - test-pipeline
  - step
  - author-test-substrate
  - vela-substrate-authoring
  - talos-engineering-compose
---

# author-test-substrate — test-pipeline step 2

<!-- nav-block:start -->
**📍 Vault Path:** [tropo-work](b8e5f3a2.md) → [test-pipeline](da3f50dc.md) → [execute — test-pipeline stage](2e097029.md) → **author-test-substrate — test-pipeline step 2**

**🔗 This file** — UID `d3511487` · type `pipeline` · state `active` · status `active`

**↔ Siblings (1):**
  - **under [execute — test-pipeline stage](2e097029.md):** [run-test-substrate — test-pipeline step 3](05d9ecc5.md)

**📥 Cited by (1):**
- [Tropo-OS v1.51.0 — Three-Pipeline Substrate-Engineering (Six A...](b0435ff0.md) — `b0435ff0` (type `release`, via `capabilities_touched`)
<!-- nav-block:end -->

**Relations**

| Relation | Target |
|---|---|
| Governed by | [pipeline (e4c8a6b2)](e4c8a6b2.md) |
| Member of | [execute — test-pipeline stage (2e097029)](2e097029.md) |

*Substrate authoring step. Vela primary; Talos composes when machine_executable_script requires non-trivial engineering (e.g., harness extension or new runner). Argus reviews substrate-coherence as separate-context verifier per pipeline.capsule v3.0 verification-class pattern.*

**Why this step's owner is split:** most test substrate is authored by Vela (validator extensions, deterministic assertion DSL expressions, agentic_review commissioning records, manual_walk prompts). Machine-executable scripts requiring meaningful engineering implementation (e.g., wiring a new test runner, instrumenting a code path for assertion capture) compose with Talos T9 per the engineering-lane pattern. Argus reviews substrate-coherence at separate context to catch box-checking misses Vela's authoring context can't see.

**What it does:**
1. For each plan section from step 1, author the test substrate per verification_method:
   - machine_executable_script → write .py/.sh/.ts file at named path; ensure exits non-zero on behavior break; pair-call Talos if engineering complexity warrants
   - structural_check → author validator extension function in tropo-validate.py; register in main() dispatcher
   - deterministic_assertion → encode DSL expression in plan substrate; engine evaluates at step 3
   - agentic_review → prep commissioning record file at agents/sa/<sa.class>/activation-log/NNN-record.md; populate [PENDING] block; dispatch fires at step 3
   - manual_walk → encode walk prompt in plan substrate; Mike walks at step 3 if dispatched
2. If harness_framework_changes_required flagged at step 1: cycle scope EXPANDED — Vela authors harness extension substrate; Talos engineers; Argus reviews capsule/spec amendments. Step 2 does NOT close until harness extension landed + working.
3. Argus reviews substrate-coherence via argus-vela channel (separate context per verification-class pattern); catches anti-box-checking misses

**Anti-box-checking discipline:** if you find yourself writing a "test" that doesn't have a meaningful pass/fail, the substrate is wrong — go back to step 1 and refine the verification_method choice. The capsule's anti-box-checking gate enforces this at step 4; catching it at authoring time is cheaper.

**Hands off to:** step 3 run-test-substrate.

## DSL rewrite note (Talos, 2026-07-04, 4f87d056/0fa72100)

Checked against `parse_exit_criterion` — all 6 lines are prose and would error. Left UNCHANGED rather than force a conversion: every line describes a dynamic, per-behavior, often-conditional check (real files exist for WHICHEVER `behaviors_covered` entries use `machine_executable_script`, a validator extension was authored for WHICHEVER use `structural_check`, etc.) — none reduces to a single static `field == value` / `exists` / `populated` check, and the grammar has no per-item iteration or per-verification_method dispatch shape. This is the SAME class 062c5544 already flagged as needing genuine engine capability (a validator-dispatch/aggregate-over-dynamic-list verb), not a syntax rewrite. Low urgency: this step is `verification_class: false`, so — like `846a0909` — these criteria are never DSL-evaluated by the engine today (`verify-step` auto-passes vc:false steps unconditionally; the `action_complete_workflow` B3-recompute only touches vc:true steps), so leaving them as prose causes no parse-error blocker. Flagging for Argus rather than silently reformatting into DSL-shaped text that would still be semantically hollow.
