---
uid: 008b86cc
title: close-loose-ends — web-pipeline step 5
type: pipeline
subtype: workflow-node
name: close-loose-ends
version: '1.0'
author: talos-t35
owner: argus
status: active
state: active
role: step
created: 2026-07-22
created_by: talos-t35
modified: 2026-07-31
modified_by: talos-t37
schema_version: 2
extraction_scope: ship
governed_by: e4c8a6b2
children: []
step_owner_role: argus
step_verifier_role: same-as-executor
verification_class: true
verification_command: >-
  python3 -c "import sys,yaml,pathlib;
  a=yaml.safe_load(pathlib.Path('vault/files/{activation}.md').read_text().split('---',2)[1]);
  r=a.get('activation_root_project');
  f=yaml.safe_load(pathlib.Path('vault/files',r+'.md').read_text().split('---',2)[1]) if r else {};
  sys.exit(0 if f.get('state')=='archived' else 1)"
depends_on_steps:
  - 1f6c4a9d
exit_criteria:
  - activation_root_project.state == archived
trust_level: standard
next_steps: []
relationships:
  - rel: member_of
    uid: 84d15fb9
member_of:
  - 84d15fb9
tags:
  - pipeline
  - workflow-node
  - web-pipeline
  - step
  - close-loose-ends
  - terminal-step
  - housekeeping
  - archival
  - durable-closure
---

# close-loose-ends — web-pipeline step 5

<!-- nav-block:start -->
**📍 Vault Path:** [tropo-work](b8e5f3a2.md) → [web-pipeline](7b2e94c1.md) → [deploy — web-pipeline stage](84d15fb9.md) → **close-loose-ends — web-pipeline step 5**
<!-- nav-block:end -->

## Purpose

The web-pipeline had no terminal close-out sweep — its prior terminal step ([smoke-test-live (1f6c4a9d)](1f6c4a9d.md)) flipped the activation root by honor-system prose (`state: standing → done`), which nothing enforced and which never reached `state: archived`. This ports the same terminal close step the dev-pipeline ([8a476130](8a476130.md)) and test-pipeline ([547e0cac](547e0cac.md)) carry, so web-pipeline closes symmetrically.

Authored under dev-spec [c392d833](c392d833.md) (durable pipeline closure; activation `63988cfb`; metis-g91 diagnosis [1d689277](1d689277.md); Argus A136), the cycle that welds root-closure to the ship gesture (pipeline.capsule §Rule 12). Primary closure is the guaranteed weld — `run_close_out_hook` archives the root + stamps `final_commit`; this declared step is web-pipeline's terminal record of that invariant + the loose-ends sweep. The `tropo-sweep-stale-roots.py` sweep (metis-g91) remains the backstop.

## Structure

Leaf node — `role: step`, `children: []`, and terminal in its stage (`next_steps: []`, so nothing follows it).

```
web-pipeline (7b2e94c1)
└── deploy — stage (84d15fb9)
    ├── commit-and-push — step 3 (e8d162b3)
    ├── smoke-test-live — step 4 (1f6c4a9d)
    └── close-loose-ends — step 5 (008b86cc)   ← this node, terminal
```

Execution reaches this node by the flow edge rather than the tree: `smoke-test-live`'s `next_steps:` carries `[008b86cc]`, and this node declares `member_of: 84d15fb9` upward. The stage's own `children:` array still lists only steps 3 and 4, so the downward half of that membership is unwritten — a runner following `next_steps:` arrives here, while anything that walks the stage's children does not see this node.

## Nodes

No child WorkflowNodes. The child table the capsule asks for is empty by construction, not unwritten: `role: step` with `children: []` is a leaf. Upward it belongs to the deploy stage ([84d15fb9](84d15fb9.md)); its only sibling edge is inbound, from `smoke-test-live — step 4` ([1f6c4a9d](1f6c4a9d.md)).

## Flow Rules

**Activates when:** step 4 `smoke-test-live` ([1f6c4a9d](1f6c4a9d.md)) reaches status:done — single-input handoff (`depends_on_steps: [1f6c4a9d]`).

**Completes when:** the declared exit criterion passes — `activation_root_project.state == archived`, evaluated by this node's `verification_command` against the activation's `activation_root_project`.

**This is now the terminal step.** No `next_steps`. Pipeline-run completes after this step verifies `activation_root_project.state == archived`.

## Cold-Boot Walk-Through

The live smoke-test passed (step 4). Before the pipeline-run terminates, the executor sweeps up everything that fed into this cycle:

1. **Find the loose ends.** Every task, note, and design-brief linked via `member_of:`/`refs:` to this activation's work-items.
2. **Close what is genuinely done.** Close each per its own capsule's terminal transition. **Do not force-close anything still legitimately open or ambiguous — leave it and note it in the closing summary.**
3. **Archive what is closed.** Every task/note/design-brief closed in step 2 (or already closed but still `state: active`) gets archived via `python3 vault/tools/tropo-archive.py <uid> --reason "cycle <this-activation-uid> closed, swept at close-loose-ends"`.
4. **Archive the Activation Root project.** This web-pipeline run's own scaffolding project gets archived — the durable-closure weld flips it to `state: archived` + stamps `final_commit` on ship; this step's verification confirms the root reached `state: archived` (superseding the old `standing → done` prose flip).
5. **Record the sweep.** A brief summary of what got closed/archived (and what was deliberately left open) goes into this pipeline-run's own closing substrate.

## Amendment to smoke-test-live

[smoke-test-live (1f6c4a9d)](1f6c4a9d.md)'s `next_steps:` is amended from `[]` to `[008b86cc]` in the same gesture that authored this step (additive only, no existing semantics changed). Both entries are Talos-owned web-pipeline substrate; no cross-agent authorization needed.

## Known Enforcement Gaps

The declared exit criterion covers action 4 only — that the Activation Root project reached `state: archived`, which the durable-closure weld guarantees on ship. Actions 1, 2, 3 and 5 (find the loose ends, close what is genuinely done, archive it, record the sweep) are executor judgment with no machine check, so a run can verify green having archived the root and swept nothing.

`tropo-sweep-stale-roots.py` (metis-g91) remains the backstop for runs that crash or ship outside the weld; it archives stale roots but performs no loose-ends sweep of its own.

## Changelog

| Version | Date | Change | Author |
|---------|------|--------|--------|
| 1.0 | 2026-07-22 | Initial authoring. Terminal close-out sweep ported into web-pipeline under dev-spec c392d833 (durable pipeline closure); `smoke-test-live` (1f6c4a9d) amended to chain here. | talos-t35 |

---

*close-loose-ends | step WorkflowNode | deploy stage (84d15fb9) | web-pipeline (7b2e94c1) | author talos-t35, 2026-07-22, durable pipeline closure (dev-spec c392d833)*
