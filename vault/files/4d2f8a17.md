---
uid: 4d2f8a17
title: "app-deploy-16 — CLAUDE.md drift walk (engineering substrate refresh)"
type: project
name: "app-deploy-16"
status: done
state: archived
owner: talos-t6
created: 2026-05-16
modified: 2026-05-30
created_by: talos-t6
modified_by: metis-g62
member_of:
  - "2918e3b4"   # app-pipeline
relationships:
  - rel: member_of
    uid: 2918e3b4
changes_in_scope:
  - "CLAUDE.md"
theme: "claude-md-drift-walk-engineering-substrate-refresh"
expected_visual_diff: "zero — docs-only; no app code touched"
target_branch: "main"
target_remote: "tropo-ai/tropo-ai"
live_url: null
schema_version: 2
extraction_scope: ship
file_ext: md
tags: [project, activation-root, app-deploy, app-pipeline, docs, claude-md, substrate-stability]
board_hygiene_note: "Board-hygiene close by Metis G62 2026-05-30 per Mike-G62 cleanup directive. Verified complete/stale via read-only audit (final_commit markers / shipped evidence / stale-activation closure). Operational substrate; no writing touched."
---

# app-deploy-16 — CLAUDE.md drift walk

<!-- nav-block:start -->
**📍 Vault Path:** [tropo-work](b8e5f3a2.md) → [app-pipeline](2918e3b4.md) → **app-deploy-16 — CLAUDE.md drift walk (engineering substra...**

**🌳 Tropo-Nav Path** (VS Code): [../../00-tropo-nav/00-tropo-all/tropo-work/app-pipeline/app-deploy-16 — CLAUDE.md drift walk (engineering substrate refresh)/4d2f8a17 — app-deploy-16 — CLAUDE.md drift walk (engineering substrate refresh).md](../../00-tropo-nav/00-tropo-all/tropo-work/app-pipeline/app-deploy-16%20%E2%80%94%20CLAUDE.md%20drift%20walk%20%28engineering%20substrate%20refresh%29/4d2f8a17%20%E2%80%94%20app-deploy-16%20%E2%80%94%20CLAUDE.md%20drift%20walk%20%28engineering%20substrate%20refresh%29.md)

**🌳 Tropo-Nav Path** (chat): [argo-os/00-tropo-nav/00-tropo-all/tropo-work/app-pipeline/app-deploy-16 — CLAUDE.md drift walk (engineering substrate refresh)/4d2f8a17 — app-deploy-16 — CLAUDE.md drift walk (engineering substrate refresh).md](argo-os/00-tropo-nav/00-tropo-all/tropo-work/app-pipeline/app-deploy-16%20%E2%80%94%20CLAUDE.md%20drift%20walk%20%28engineering%20substrate%20refresh%29/4d2f8a17%20%E2%80%94%20app-deploy-16%20%E2%80%94%20CLAUDE.md%20drift%20walk%20%28engineering%20substrate%20refresh%29.md)

**🔗 This file** — UID `4d2f8a17` · type `project` · state `archived` · status `done`

**↔ Siblings (33):**
  - **under [app-pipeline](2918e3b4.md):** [01-inbox — app-pipeline](46dfbb0a.md) · [app-deploy-1 — UI Foundation Step 1a + 1b](52dadffd.md) · [app-deploy-10 — Decompose app/projects/[id]/pag...](c8ab4772.md) · [app-deploy-11 — Minimum-viable smoke-test layer...](7f3d8c91.md) · [app-deploy-12 — eslint --fix auto-cleanup (part...](6e1f4b73.md) · [app-deploy-13 — Next.js framework migrations + ...](9b4d2e58.md) · + 27 more
<!-- nav-block:end -->

**Relations**

| Relation | Target |
|---|---|
| Member of | [app-pipeline (2918e3b4)](2918e3b4.md) |

## What ships

The engineering substrate every AI coding agent reads at session start. CLAUDE.md walked end-to-end; six sections materially out of sync with the post-T5+T6 codebase corrected. Net change: +128 / −68 lines (140 → 199).

### Sections updated

**Stack** — added the `--tropo-*` token system note (legacy tokens retired in app-deploys 1–3). Pointed at `app/styles/tropo-tokens.css` as canonical. Pointed at `npm test` as the smoke-test entry.

**Project Structure** — full rewrite. Brand corrections (Mo Author → Tropo Author throughout). Added every file that landed in T5+T6 that the doc didn't know about:
- `app/layout.tsx` (shared AppShell)
- `app/api/health/route.ts`
- `app/api/{crew,projects,artifacts,docx,pdf,handoff,upload,test}/`
- `components/ui/` (the 8 primitives)
- `components/crew/*` (5 decomposed sub-components)
- `lib/app-shell-context.tsx`
- `lib/vault-project-index.ts` (extracted in app-deploy-11 so it's testable)
- `lib/use-chat-session.ts` + `lib/use-project-chat-session.ts`
- `lib/sse-parser.ts`
- `lib/query-rewriter.ts`, `lib/kb-discovery.ts`, `lib/kb-indexer.ts`, `lib/debug.ts`, `lib/sprint-tests.ts`
- `tests/` (the smoke-test layer)
- `scripts/` (platform-tier TS — Talos's lane per A63 clarification)

Marked `kb/` as legacy (post-vault-migration; contains only `kb-config.md` + `.kb-index.json` now).

**Coding Conventions** — corrected the "SSE parsing is done in `page.tsx`" claim (it's now in `lib/sse-parser.ts` shared by both chat hooks + the test harness). Added the `useHeaderSlots` pattern for per-page chrome. Added "internal nav uses `<Link>`, images use `<Image>`" post-app-deploy-13. Tightened the `any` rule to "no `any`" (every `no-explicit-any` error closed in app-deploys 12 + 14).

**The Query Pipeline** — added the `vault-project-index.ts` reference for the 2-hop resolver (was hidden inside `intent-extraction.ts` pre-app-deploy-11).

**What Not to Break** — replaced `kb/.kb-index.json` with `argo-os/vault/00-index.jsonl` (the canonical post-migration index). Replaced "do not write to argo-os without Argus direction" with a properly-scoped pointer to the §the Studio section that explains what engineering CAN and CANNOT write post-ADR-040.

**Known Technical Debt** — closed `app/page.tsx is 822 lines` (decomposed in app-deploy-8 to ~100 lines via `useChatSession`). Closed `Mo / MindBridge brand naming` (app-deploy-7 brand sweep). Added the 39 lint warnings carry-forward (5e3a82c1) and the 9 CSS tokens carry-forward (53a7a624). Added "no CI yet" so future agents see it explicitly.

**the Studio (renamed from "The Vault")** — major rewrite. Post-ADR-040 substrate tracking declared. New subsections:
- "What engineering CAN write into the vault" — inbox findings, activation roots, session snapshots, sa.* files. Concrete UID examples.
- "What engineering does NOT write" — capsules, ADRs, governance, Mike-signed primitives, other agents' identity substrate.

Corrected the vault registry path from `argo-os/registry.yaml` (legacy) to `argo-os/.tropo-studio/registries/registry.yaml` (canonical post-v1.9.1 + the path fix Talos T6 shipped as inbox finding 8f3c2d51 closure).

**Environment** — full test surface enumerated (test / test:qa / test:qa:dev / test:qa:live / test:qa:all). Added vault rebuild + registry rebuild + validate.

**Team** — corrected Mike's title from "CEO" → "Founder" (per T3 memory pin). Tightened "Non-engineer" to "Reads code; not a coder" (more accurate). Talos line now references the sa.* fleet of 7 explicitly. Vela line names the captain-mode commit-cadence per ADR-040. Added Cosmo to the roster.

## Acceptance

- Docs-only — no code touched. typecheck unaffected; smoke tests unaffected.
- Stranger-encounter pass: a fresh agent booting and reading CLAUDE.md should now describe the codebase accurately (5 page files mentioned, 8 primitives, AppShell, smoke-test surface, vault-in-git, fleet).
- Every claim in the new version cross-checks against current substrate (verified via `grep` / `ls` against the live tree before authoring).

## Why now

Per Mike-T6 2026-05-16 directive ("Let's get tropo clean and then we move onto the website"): CLAUDE.md was the highest-leverage stability gap. Every engineering agent reads it at boot; stale claims compound into bad decisions over generations. The Founder/CEO drift was a specific pin from T3 memory that survived 30+ generations because nobody walked the doc.

If we don't fix it now, when?

---

*app-deploy-16 | activation root | UID 4d2f8a17 | docs-only, substrate-stability cleanup*
