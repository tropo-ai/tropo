---
uid: 674af8fe
title: pre-author-release-entry — dev-pipeline step 5.5
type: pipeline
subtype: workflow-node
name: pre-author-release-entry
version: 1.0.0
author: argus-a77
owner: argus
status: draft
state: active
role: step
children: []
step_owner_role: argus
step_verifier_role: same-as-executor
verification_class: false
depends_on_steps:
- 804e339e
exit_criteria: []
intent_criteria_v1_48_target:
- last_artifact.uid exists
- last_artifact.release_version exists
- last_artifact.foundation exists
- last_artifact.draft_pre_ship_note exists
- last_artifact.type == release
trust_level: auto-with-verification
timeout_hours: 1
compensation_step_id: null
v1_47_engine_contract: 'step 5.5 fires substrate-resident via canonical convention: Argus authors release-entry → fires step-complete with --artifact-links <new-uid> → engine accepts as completed (trivial-pass on empty exit_criteria). The intent-criteria above are v1.48 engine-enhancement target (requires `last_artifact` context handle resolving from prior step''s artifact_links emission); for v1.47 the convention IS the verdict. d.fresh-reader R3 P0.1 absorption: exit_criteria rewritten from prose-shaped to DSL-honest empty + intent-criteria documented separately.'
v1_48_0_provenance: 'Authored 2026-05-20 by argus-a77 under v1.48.0 cycle Stream D per cycle brief [c184b781 v0.3 LOCKED] §3 Stream D + V48 argus-vela channel flag 2026-05-20: ''the dev-pipeline retrofit step entries don''t include a pre-author release entry stub step. v1.43-v1.45 had this implicit in Argus''s pre-flight cycle work; v1.46''s structural fix shipped the engine without codifying the release-entry-authoring as a substrate-resident dev-pipeline step.'' Formalizes the release-entry stub authoring at cycle pre-flight (was memory-resident Argus behavior across v1.43-v1.46; v1.48 makes it substrate-resident so pipeline-runs structurally cannot skip the gate). Position 5.5 between 804e339e (generate-release-notes) and 8654900a (produce-release-folder) per cd1fcd25 root structure diagram. Note: 804e339e.next_steps still points at hanging step a5554670 (groom-subsystems; never wired into deploy.children: array per cycle brief R1 absorption); that substrate-inconsistency is flagged for v1.50 grooming and out-of-scope for v1.48 Stream D. This step''s depends_on_steps + 8654900a''s depends_on_steps amendment land the actual flow via the canonical chain. Substrate originally authored under v1.47.0 cycle label; numbering shifted v1.47 → v1.48 per Mike-V49 chain-mutability override 2026-05-21 (Vela V49''s substrate-discipline interrupt cycle took v1.47.0 release tag at 3dbc7d88; my Cycle B + C + D substrate shifted +1).'
next_steps:
- 9d4f7e21
relationships:
- rel: member_of
  uid: 3a7dbdda
schema_version: 2
extraction_scope: ship
member_of:
- 3a7dbdda
subsystem_hub:
- 76bab75f
capsule_version: '2.5'
---

# pre-author-release-entry — dev-pipeline step 5.5

<!-- nav-block:start -->
**📍 Vault Path:** [tropo-subsystems](aae9a37b.md) → [Tropo Playbooks](76bab75f.md) → **pre-author-release-entry — dev-pipeline step 5.5**

**🔗 This file** — UID `674af8fe` · type `pipeline` · state `active` · status `draft`

**↔ Siblings (5):**
  - **under [deploy — dev-pipeline stage](3a7dbdda.md):** [cold-boot-walk — dev-pipeline step 7.5](c6b61fb9.md) · [groom-subsystems — dev-pipeline step (NEW v1.7)](a5554670.md) · [notify-triggered-pipeline-owners — dev-pipeline...](37996741.md) · [trigger-doc-pipeline-activation — dev-pipeline ...](0cf86ea5.md) · [trigger-test-pipeline-activation — dev-pipeline...](4f64ec3c.md)

**📥 Cited by (2):**
- [Tropo-OS v1.48.0 — Cycle B Extraction-and-Publish Engineering ...](1d25e142.md) — `1d25e142` (type `release`, via `capabilities_touched`)
- [dev-pipeline step-4 + step-5.5 deps mismatch — update-subsyste...](74f84c63.md) — `74f84c63` (type `note`, via `refs`)
<!-- nav-block:end -->

**Relations**

| Relation | Target |
|---|---|
| Member of | [deploy — dev-pipeline stage (3a7dbdda)](3a7dbdda.md) |

## Purpose

Author the release-entry stub at cycle pre-flight, before the release folder gets produced and external tests run. The release-entry is governance-class substrate; it MUST exist with structural fields populated BEFORE the build artifact gets cut, so post-ship remediation_history + foundation citations land on a real vault entry rather than retroactively reconstructing the entry post-ship.

Closes the gap V48 surfaced in argus-vela 2026-05-20: through v1.43-v1.46, release-entry stub authoring was memory-resident Argus behavior at cycle pre-flight — implicit, ungoverned, sometimes skipped. v1.47 codifies the step substrate-resident so the engine structurally cannot advance past step 5.5 without a real release-entry UID landing in vault.

Does NOT govern: the release-entry's full body content (that lands at cycle close via remediation_history accrual + post-ship lock-fields). The stub at this step is the structural scaffold; the body accumulates across the cycle through cycle-execution acts.

## Structure

Leaf step. No children.

## Nodes

No child nodes. This is a leaf WorkflowNode.

## Flow Rules

**Completes when:** Argus (step_owner_role) authors a new vault entry at `vault/files/<new-uid>.md` with `type: release` + the four intent-criteria fields populated (these are the v1.48+ engine-enhancement DSL target; at v1.47 ship the engine fires trivial-pass on empty exit_criteria + the convention IS the verdict):

- `last_artifact.uid` resolves (the just-authored entry has a valid 8-hex UID matching its filename)
- `last_artifact.release_version` exists (e.g., `release_version: "1.47.0"`)
- `last_artifact.foundation` exists (UID array citing the cycle brief + design-specs informing this release; e.g., `[c184b781, 6a8d3f17]`)
- `last_artifact.draft_pre_ship_note` exists (concise prose describing what the cycle scope is + what's expected at ship-time)
- `last_artifact.type == release`

The entry is at `state: draft` when this step completes; it will transition to `state: locked` post-step-7 (git-commit) by Vela's ship-discipline at cycle close.

**verification_class: false** — Argus authors the substrate; the substrate's existence + field-presence is the verdict. At v1.47 ship: engine treats step-complete-fire as the verdict (no DSL evaluation since exit_criteria is empty per the §Engine Contract below). At v1.48+: engine enhancement adds `last_artifact` context handle for mechanical DSL evaluation against the intent-criteria.

**Advances to:** produce-release-folder (8654900a) on completion.

## Engine Contract — v1.47 vs v1.48+

**v1.47 ship (convention-by-fire):** The engine's DSL grammar at `pipeline-runtime.py:439` (`parse_exit_criterion`) supports five shapes against three context handles (`activation`, `pipeline`, `activation_root` per `build_run_context_uids` at line 575). There is NO `last_artifact` handle for resolving the just-authored release-entry UID emitted via `step-complete --artifact-links <new-uid>`. At v1.47, step 5.5 fires via:

```bash
# Argus authors vault/files/<new-uid>.md with type:release + 4 required fields
npm run vault:rebuild
python3 .tropo/scripts/pipeline-runtime.py \
  --activation-uid <activation-uid> --actor argus-a<N> \
  step-complete 674af8fe --artifact-links <new-uid>
python3 .tropo/scripts/pipeline-runtime.py \
  --activation-uid <activation-uid> --actor argus-a<N> \
  verify-step 674af8fe
# verify-step returns 'pass' (trivial-pass on empty exit_criteria per :922-923)
```

The convention IS the verdict at v1.47: Argus's discipline ensures the four intent-criteria are met before firing step-complete; the engine doesn't mechanically enforce.

**v1.48+ engine enhancement target:** Add `last_artifact` context handle to `build_run_context_uids` that resolves to the most recent step's emitted `artifact_links[0]` UID. Then the intent_criteria_v1_48_target frontmatter field can move to the canonical `exit_criteria:` array + the engine evaluates mechanically:

- `last_artifact.uid exists` → uid_exists check against the resolved UID
- `last_artifact.release_version exists` → field_equals or field_contains check
- `last_artifact.foundation contains <cycle-brief-uid>` → field_contains check
- `last_artifact.draft_pre_ship_note exists` → field_equals/uid_exists pattern
- `last_artifact.type == release` → field_equals

The engine enhancement is a v1.48 substrate-resident discipline ratchet alongside Stream B-style engineering deltas. Filed as v1.48 carryalong; this step entry's `intent_criteria_v1_48_target:` field is the substrate-spec for that enhancement.

## v1.47 Dogfood Note

Step 5.5 was authored within v1.47's own cycle. v1.47's release-entry stub at [1d25e142](1d25e142.md) was authored captain-mode by Argus A77 ahead of step 5 — the substrate dogfoods its own discipline before step 5.5 is part of any pipeline-run's step list (v1.47's own run a9750384 was bootstrapped before step 5.5 existed; v1.48+ cycles will fire step 5.5 mechanically through the engine as part of the canonical pipeline flow).

## Cold-Boot Walk-Through

A pipeline-run enters step 5.5 with:
- step-4 (180e9108 generate-release-plan) closed; release-plan substrate exists at vault/files/<plan-uid>.md
- step-5 (804e339e generate-release-notes) closed; release-notes substrate exists at vault/files/<notes-uid>.md

Argus (step_owner_role) authors the release-entry:

1. Generate new 8-hex UID for the release-entry (collision-check against existing vault/files/).
2. Author `vault/files/<new-uid>.md` with frontmatter populating the four exit_criteria fields:
   - `type: release`
   - `release_version: v<X.Y.Z>` (matches activation contract version)
   - `foundation: [<cycle-brief-uid>, <design-spec-uids>, ...]`
   - `draft_pre_ship_note: "<concise cycle-scope summary>"`
3. Optionally populate additional release.capsule schema fields (capabilities_touched, narrative_provenance, etc.) — those accumulate through the cycle.
4. Rebuild vault index (npm run vault:rebuild) to register the entry.
5. Fire `pipeline-runtime.py step-complete 674af8fe --artifact-links <new-uid>`.
6. Fire `pipeline-runtime.py verify-step 674af8fe` — at v1.47 ships trivial-pass on empty exit_criteria (the convention IS the verdict; Argus's discipline ensures the four intent-criteria fields are present). At v1.48+, engine evaluates intent_criteria mechanically against the new entry via `last_artifact` context handle.

The engine accepts the convention at v1.47: Argus authors substrate cleanly + fires step-complete; the substrate-discipline IS the verdict. v1.48 engine enhancement (filed as carryalong) adds mechanical DSL evaluation against the intent-criteria.

## Composes-With

- **release.capsule (b19e8d43)** — governs the release-entry's schema. This step authors against that capsule's contract.
- **804e339e (generate-release-notes step 5)** — gates this step; release-notes content informs the release-entry's draft_pre_ship_note + foundation citations.
- **8654900a (produce-release-folder step 6)** — downstream gate; release-entry UID feeds into the build artifact's release.md authorship.

## v1.47.0 Provenance

Authored Argus A77 2026-05-20 captain-mode under v1.47.0 cycle Stream D per:

- V48 verbatim flag in argus-vela.md 2026-05-20: *"the dev-pipeline retrofit step entries don't include a 'pre-author release entry stub' step. v1.43-v1.45 had this implicit in Argus's pre-flight cycle work; v1.46's structural fix shipped the engine without codifying the release-entry-authoring as a substrate-resident dev-pipeline step."*
- Cycle brief [c184b781 v0.3 LOCKED](c184b781.md) §3 Stream D
- R1 paired gauntlet absorbed Stream D positioning per cd1fcd25 root structure diagram

Substrate landed atomically with:
- Deploy stage (3a7dbdda) `children:` array amendment extending to include 674af8fe between 804e339e and 8654900a
- 804e339e `next_steps:` amendment: [a5554670 — hanging] → [674af8fe] (closes the hanging-step substrate-inconsistency for the canonical flow; a5554670 itself remains for v1.49 grooming)
- 8654900a `depends_on_steps:` amendment: [804e339e] → [674af8fe]
- Dev-pipeline root (cd1fcd25) `v1_47_0_changes:` annotation

## Out-of-scope (v1.49 grooming carryalong)

The hanging step a5554670 (groom-subsystems v1.7) — authored as a step between 804e339e and 8654900a at v1.7 but never wired into deploy stage's `children:` array. Empirical truth: groom-subsystems is a hanging step-entry not currently active in the dev-pipeline flow per cd1fcd25 root structure diagram. v1.47 Stream D positions pre-author-release-entry per the canonical structure diagram; the groom-subsystems wiring inconsistency stays a separate substrate-defect for v1.49 grooming (either complete the v1.7 wiring OR archive a5554670 as never-activated).

## Changelog

| Version | Date | Change | Author |
|---------|------|--------|--------|
| 1.0.0 | 2026-05-20 | Initial authoring. v3.0 per-step rich schema. verification_class:false (Argus authors substrate; field-presence IS the verdict). step-5.5 position between generate-release-notes and produce-release-folder. | argus-a77 |

---

*pre-author-release-entry | step WorkflowNode | deploy stage (3a7dbdda) | dev-pipeline (cd1fcd25) | argus-a77 | 2026-05-20*
*"Release-entry stub exists before the artifact ships. Substrate-resident, not memory-resident."*
