{
  "name": "tropo-os-studio",
  "version": "1.33.0",
  "description": "Tropo-OS Studio — the L1 starter template shipped to users.",
  "private": true,
  "scripts": {
    "test": "python3 vault/tools/tropo-test.py",
    "test:json": "python3 vault/tools/tropo-test.py --json",
    "test:verbose": "python3 vault/tools/tropo-test.py --verbose",
    "vault:rebuild": "python3 vault/tools/tropo-rebuild-vault.py --apply"
  }
}
