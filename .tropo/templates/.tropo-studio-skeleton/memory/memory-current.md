---
scope: vault
memory_schema_version: 3.0
last_compacted: null
compacted_by: null
note: "Fresh Studio install — vault-level memory at v3 substrate scaffold. First executive boot dispatches sa.memory-curator (trigger=boot or trigger=explicit) which regenerates this file as a curated pointer-index of tier=current entries. The Top-of-Mind / Topics / Historical sections below populate as the substrate accumulates."
---

# Vault-Level Memory — Current (Pointer Index)

*Crew-class pins read by every executive at boot per Tier 2 boot protocol. Distinct from per-agent memory at `agents/<name>/.tropo-capsule/memory/`. **Fresh Studio install scaffold** — first executive boot dispatches sa.memory-curator which regenerates this file.*

## Top-of-Mind (tier=current)

- [839a65f9](entries/839a65f9.md) — Deletion Discipline — Never destroy governed substrate; always soft-delete via `tropo-recycle.py` *[CREW-WIDE; ships with every Tropo Studio at v1.41+; canonical doctrine at `vault/files/0aefe71d.md`]*

## Topics (tier=topic; lazy-loaded)

*Empty at fresh install. Populates as the substrate accumulates pins via sa.memory-curator dispatches.*

## Historical (tier=archival)

*Empty at fresh install. Snapshots in `history/` as memory ages across generations.*

---

*Vault-Level Memory v3 Scaffold | Template | Regenerated by sa.memory-curator at boot*
*Ships with every Tropo Studio install via `.tropo-studio-skeleton/memory/` template per `build-release.py` step_7_create_vault_skeleton.*
