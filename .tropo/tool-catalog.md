---
uid: tool-catalog
name: tool-catalog
type: catalog
kind: tool
generated_at: 2026-07-11
generated_by: generate-capability-catalogs.py (v1.15)
source: vault/00-index.jsonl filtered by type:tool + extraction_scope:ship
governed_by: d5e1b4a3   # tool.capsule v1.4
extraction_scope: ship
---

# Tropo Tool Catalog

*Auto-generated 2026-07-11 from vault entries with `type: tool` + `extraction_scope: ship`. Hand-authored content lives in each entry's `trigger_description:` field. Do not edit this file directly — re-run the generator instead.*

## How to invoke

Tools are **typed callables**. The Invocation block under each entry is the executable command. Default working directory is the Studio root (the directory containing `vault/` + `.tropo/`). Exit code 0 = pass; non-zero = drift or failure (each entry's Failure Modes section in the underlying vault entry — linked by UID — documents the specific exit-code contract).

**50 tools** registered in this Studio.

---

## archive() — flip entry state active↔archived with provenance + event

**Domain.** Lifecycle — the canonical gesture for archiving governed vault entries (Option-A state discipline, 99e52c18)

**When to reach for it.** Flip entry state active↔archived with provenance and event.

**Invocation.**
```
python3 vault/tools/tropo-archive.py <uid> --reason <reason> [--superseded-by <uid>] [--unarchive] [--force-with-reason <reason>] [--actor <label>]
```

**Implementation.** [vault/tools/tropo-archive.py](../vault/tools/tropo-archive.py)

**UID.** [`6cc9dcdb`](../vault/files/6cc9dcdb.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## build-release — Tool

**Domain.** Build a Tropo-OS release from the Argo vault — versioned folder, manifest, index, ship artifacts.

**When to reach for it.** Reach for this when executing Stream I (build + ship) of any dev-pipeline cycle. Run with --bump patch (e.g., v1.13.x → v1.14.0 isn't patch — that's feature; v1.14.0 → v1.14.1 IS patch), --bump feature (v1.14.0 → v1.15.0), or --bump release (v1.14.0 → v2.0.0). Step 0 + Step 0b pre-flight gates run validators automatically and refuse to build on validator failures. Use --dry-run to preview the build without writing. Use --force only on retries after fixing the conflict that triggered the overwrite guard.

**Invocation.**
```
python3 vault/tools/tropo-build-release.py --bump <patch|feature|release> [--dry-run] [--force]
```

**Implementation.** [vault/tools/tropo-build-release.py](../vault/tools/tropo-build-release.py)

**UID.** [`a1b8c2d4`](../vault/files/a1b8c2d4.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## build-web-content

**Domain.** Build the Tropo website-content extraction from the Argo vault.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/11f3ebd4.py
```

**Implementation.** [vault/tools/11f3ebd4.py](../vault/tools/11f3ebd4.py)

**UID.** [`11f3ebd4`](../vault/files/11f3ebd4.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## cascade-stress-compare

**Domain.** R3 cascade-runtime-stress: structural-shape comparison of two cascade runs.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/43b792b5.py
```

**Implementation.** [vault/tools/43b792b5.py](../vault/tools/43b792b5.py)

**UID.** [`43b792b5`](../vault/files/43b792b5.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## check-events — identity-resolved event drain (directed + broadcasts; cannot miss a reply_required)

**Domain.** Identity-resolved event drain

**When to reach for it.** Drain your event log (directed + broadcasts; cannot miss a reply_required).

**Invocation.**
```
python3 vault/tools/tropo-check-events.py --as <name>
```

**UID.** [`2471edc0`](../vault/files/2471edc0.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## check-one

**Domain.** Targeted vault entry validation — exit-coded for verification_command use

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/tropo-check-one.py <uid> [--capsule <name>] [--vault-path <path>] [--quiet]
```

**Implementation.** [vault/tools/tropo-check-one.py](../vault/tools/tropo-check-one.py)

**UID.** [`0f3078fe`](../vault/files/0f3078fe.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## derive-subsystem-registry

**Domain.** derive-subsystem-registry.py — Going-forward subsystem-registry.jsonl deriver.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/6342d0ca.py
```

**Implementation.** [vault/tools/6342d0ca.py](../vault/tools/6342d0ca.py)

**UID.** [`6342d0ca`](../vault/files/6342d0ca.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## emit-event — Canonical Event Emission Primitive

**Domain.** Canonical event emission

**When to reach for it.** Emit a CloudEvent (message/broadcast/completion). Resolves party identity.

**Invocation.**
```
python3 vault/tools/tropo-emit-event.py
```

**UID.** [`ca90f098`](../vault/files/ca90f098.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## fix-duplicate-yaml-keys

**Domain.** fix-duplicate-yaml-keys.py — one-shot cleanup for the v1.12 dup-key class.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/34fb726c.py
```

**Implementation.** [vault/tools/34fb726c.py](../vault/tools/34fb726c.py)

**UID.** [`34fb726c`](../vault/files/34fb726c.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## generate-capability-catalogs — Tool

**Domain.** Generate three Tropo capability catalogs (tool / skill / sa-agent) from vault entries — v1.15 substrate.

**When to reach for it.** Reach for this any time the three Tropo capability catalogs need to be regenerated — typically after adding/amending vault entries with type:tool/how-to/session-agent (new capabilities, edited trigger_description, status changes that affect catalog membership). Also re-run as part of build-release.py preflight to ensure shipped Studios get fresh catalogs. Run with --apply to write; without --apply for dry-run preview that surfaces missing-trigger-description warnings without modifying files.

**Invocation.**
```
python3 vault/tools/tropo-generate-capability-catalogs.py [--apply] [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-generate-capability-catalogs.py](../vault/tools/tropo-generate-capability-catalogs.py)

**UID.** [`d4e9a2c7`](../vault/files/d4e9a2c7.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## generate-relations-header — Tool

**Domain.** Render Relations + Members navigation blocks from a file's frontmatter — UID-graph navigation surface.

**When to reach for it.** Reach for this when you need to refresh the Relations + Members navigation blocks in a vault entry's body — typically after editing the file's member_of frontmatter or any other UID-reference field. Run --write to modify in place; without --write for dry-run preview. Or run rebuild-vault.py wrapper to refresh Relations + Members across the whole vault as part of the comprehensive cadence (this script is Step 4 of that flow). The Members section uses typed-cap format (5 per type + overflow indicator) so hub entries with many children (tropo-governance has 41 decisions, 175 documents, 72 projects) stay scannable.

**Invocation.**
```
python3 vault/tools/tropo-generate-relations-header.py [--write] [--vault-root PATH] <target>...
```

**Implementation.** [vault/tools/tropo-generate-relations-header.py](../vault/tools/tropo-generate-relations-header.py)

**UID.** [`b8e4f1a3`](../vault/files/b8e4f1a3.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## generate-update-manifest — Tool

**Domain.** Generate the Update API's static manifest JSON (transport lean per fc4874f4 A122 walk) from governed type:release vault entries, and optionally upload it to the Supabase releases bucket at a stable, non-expiring URL.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/tropo-generate-update-manifest.py [--upload] [--out <path>]
```

**Implementation.** [vault/tools/tropo-generate-update-manifest.py](../vault/tools/tropo-generate-update-manifest.py)

**UID.** [`5a91c3f0`](../vault/files/5a91c3f0.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## import-walker.py — Substrate-Internal Walker

**Domain.** Substrate-internal walker for the import primitive (v1.25.0 + v1.26.0 + v1.28.0). Authors sidecars + folder markers + folder mirrors + vault projections; computes hashes via the stable-id → content-aware → sha256 fallback chain; appends to the reconciler journal.

**When to reach for it.** Reach for this when the import primitive needs substrate-internal operations — scanning a Studio for governable folders/files, authoring a sidecar + folder mirror + projection for one source file, running drift detection, applying a reconciler-event payload, or promoting a folder Tier 1 → Tier 2. Most users do NOT invoke this directly; sa.reconciler spawns it for reconciliation, and the v1.25.0 + v1.28.0 onboarding flows trigger create-sidecar implicitly. Direct invocation is for substrate-repair and CI-style scripted import. v1.28.0 v1.0.3 amendments: create-sidecar now co-writes folder-marker MIRROR at vault/files/<folder-uid>.md alongside the on-disk marker (ordered-write protocol with atomic-rename + inline index sync); populates `original_styles:` on .docx projections via shared office_styles library; supports retro-fill for pre-v1.28.0 imports; rebuilds folder mirror Members section from registry walk on every invocation.

**Invocation.**
```
python3 vault/tools/tropo-import-walker.py {scan|create-sidecar|reconcile|apply|promote-folder|extract} [...]
```

**Implementation.** [vault/tools/tropo-import-walker.py](../vault/tools/tropo-import-walker.py)

**UID.** [`bf886f30`](../vault/files/bf886f30.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## loop-activate — the human launch-gate for governed loops

**Domain.** Activation gate for governed loops (v1.71 S1).

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/2e5c81d3.py
```

**Implementation.** [vault/tools/2e5c81d3.py](../vault/tools/2e5c81d3.py)

**UID.** [`2e5c81d3`](../vault/files/2e5c81d3.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## loop-brakes-watchdog — ground-truth circuit-breaker + human checkpoints

**Domain.** The platform-enforced BRAKES circuit-breaker + human checkpoints for loop-runs (v1.71 S1).

**When to reach for it.** Launchd-fired watchdog that kills runaway loop-runs and enforces human checkpoints.

**Invocation.**
```
python3 vault/tools/1edbee15.py
```

**Implementation.** [vault/tools/1edbee15.py](../vault/tools/1edbee15.py)

**UID.** [`1edbee15`](../vault/files/1edbee15.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## migrate-sidecar-to-singlefile

**Domain.** Migrate one sidecar tool entry (vault/files/<uid>.md + .tropo/scripts/<name>.py) to single-file-truth at vault/tools/<uid>.py per tool.capsule v1.6 §2.5. Idempotent.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/c778f748.py --sidecar-uid <uid>
```

**UID.** [`c778f748`](../vault/files/c778f748.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## migrate-v14-subsystem-hub-split

**Domain.** v1.14 schema split migration — move subsystem hub UIDs from member_of: → subsystem_hub:.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/8c4cea94.py
```

**Implementation.** [vault/tools/8c4cea94.py](../vault/tools/8c4cea94.py)

**UID.** [`8c4cea94`](../vault/files/8c4cea94.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## mint-id — the canonical collision-checked identifier minter

**Domain.** Identifier minting — the one collision-checked chokepoint for governed identifiers (ADR-050)

**When to reach for it.** Get a fresh collision-checked identifier before authoring (file/agent UID or studio genesis today; vault/event declared, not yet built).

**Invocation.**
```
python3 vault/tools/tropo-mint-id.py
```

**Implementation.** [vault/tools/tropo-mint-id.py](../vault/tools/tropo-mint-id.py)

**UID.** [`5187be30`](../vault/files/5187be30.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## navblock-strip — Tool

**Domain.** I5 (dd16c90c) enforcement — sentinel-bounded strip of the <!-- nav-block:start -->...<!-- nav-block:end --> region at the git boundary (dev-spec 6ec30708, Option A).

**When to reach for it.** Reach for this when installing/verifying the nav-block git clean filter on a fresh clone or studio (--install then --verify-install), or when auditing whether any committed vault/files/*.md blob still carries a nav-block (--check).

**Invocation.**
```
python3 vault/tools/tropo-navblock-strip.py [--clean | --install | --verify-install | --check [PATH...] | --migrate PATH...]
```

**Implementation.** [vault/tools/tropo-navblock-strip.py](../vault/tools/tropo-navblock-strip.py)

**UID.** [`dd3ac1c3`](../vault/files/dd3ac1c3.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## office_styles

**Domain.** Extract structured style metadata from .docx binaries — page size + margins, default font, theme colors, named paragraph styles, headers/footers, sections count, special features (macros / embedded fonts / custom XML / watermarks).

**When to reach for it.** Reach for this when authoring a script that needs to extract Office style metadata from a .docx binary. Pass an absolute Path; the function returns a dict matching arch-spec 5a89297a §3.4 schema, or None if the file isn

**Implementation.** [vault/tools/tropo-office-styles.py](../vault/tools/tropo-office-styles.py)

**UID.** [`d1f2420d`](../vault/files/d1f2420d.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## pipeline-activate

**Domain.** pipeline-activate.py — pipeline-template activation runtime + cascade execution.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/e337f1dd.py
```

**Implementation.** [vault/tools/e337f1dd.py](../vault/tools/e337f1dd.py)

**UID.** [`e337f1dd`](../vault/files/e337f1dd.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## pipeline-runtime

**Domain.** pipeline-runtime.py — v1.46.0 sequential-execution companion to pipeline-activate.py.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/9e7003b1.py
```

**Implementation.** [vault/tools/9e7003b1.py](../vault/tools/9e7003b1.py)

**UID.** [`9e7003b1`](../vault/files/9e7003b1.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## publish

**Domain.** publish.py — Thin shared runner for publish.pipeline definitions.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/2e642578.py
```

**Implementation.** [vault/tools/2e642578.py](../vault/tools/2e642578.py)

**UID.** [`2e642578`](../vault/files/2e642578.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## publish-check

**Domain.** publish-check.py — Pre-publish readiness check for articles + ship-artifact wrappers.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/4beff0d6.py
```

**Implementation.** [vault/tools/4beff0d6.py](../vault/tools/4beff0d6.py)

**UID.** [`4beff0d6`](../vault/files/4beff0d6.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## query-events — CLI Event Log Query Wrapper

**Domain.** Historical event query

**When to reach for it.** Query historical event log with advanced filtering.

**Invocation.**
```
python3 vault/tools/tropo-query-events.py
```

**UID.** [`1545ac97`](../vault/files/1545ac97.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## rebuild-events-sqlite — Regenerate SQLite from Canonical JSONL

**Domain.** messaging

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/tropo-rebuild-events-sqlite.py
```

**UID.** [`4d20a322`](../vault/files/4d20a322.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## rebuild-index — Tool

**Domain.** Fast index + project-tree rebuild — vault/00-index.jsonl + 00-project-tree.jsonl from frontmatter (v1.15.1).

**When to reach for it.** Reach for this any time you've added/removed/amended a vault entry's frontmatter and need the index + project-tree to reflect it. Fast (~1-2s on 1460+ entries) and idempotent — safe to re-run frequently. Use this for the common-case edit-and-index cycle; for the comprehensive refresh (index + symlink rehydrate + cascade cleanup) use rebuild-vault.py instead. Run with --apply to write; without --apply for dry-run preview. Reflection-of-frontmatter means new conventions (era, voice_authority, category, audience, etc.) reach the index automatically without script updates.

**Invocation.**
```
python3 vault/tools/tropo-rebuild-index.py [--apply] [--only UID] [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-rebuild-index.py](../vault/tools/tropo-rebuild-index.py)

**UID.** [`f4b8a6e2`](../vault/files/f4b8a6e2.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## rebuild-project-index — Tool

**Domain.** Rebuild a project's 00-index.md from folder contents — 4-column table from frontmatter.

**When to reach for it.** Reach for this when you encounter a projects/<slug>/00-index.md that is missing, stale, or out of sync with the folder contents — typically while navigating to a project, registering a new file in it, or inspecting its status. Idempotent and safe to re-run; produces identical output on unchanged folders. Future v1.5+ may add watcher-triggered automation; today it is reader-triggered (the agent that notices the drift fixes it).

**Invocation.**
```
python3 vault/tools/tropo-rebuild-project-index.py --slug <project-slug> [--dry-run]
```

**Implementation.** [vault/tools/tropo-rebuild-project-index.py](../vault/tools/tropo-rebuild-project-index.py)

**UID.** [`c6e2b9d4`](../vault/files/c6e2b9d4.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## rebuild-vault — Tool

**Domain.** Comprehensive substrate refresh — wraps rebuild-index + rehydrate + stale-cascade cleanup (v1.15.1 wrapper).

**When to reach for it.** Comprehensive substrate refresh (index + nav + boards + brief).

**Invocation.**
```
python3 vault/tools/tropo-rebuild-vault.py [--dry-run] [--only UID] [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-rebuild-vault.py](../vault/tools/tropo-rebuild-vault.py)

**UID.** [`e8d4c1b9`](../vault/files/e8d4c1b9.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## register-kernel — Tool

**Domain.** Register .tropo/ kernel files in the vault index — type: kernel entries pointing at .tropo/ paths.

**When to reach for it.** Reach for this when kernel files have been added/removed/renamed under .tropo/ and the vault index needs to reflect that change. Companion to rebuild-vault.py — that one rebuilds entries authored at vault/files/<uid>.md; this one registers files that stay at their .tropo/ path. Run after kernel directory changes; not part of routine index rebuild. Future v1.16+ may merge these two into a single rebuild script per the Kernel Migration to Vault thesis (1fa26624).

**Invocation.**
```
python3 vault/tools/tropo-register-kernel.py [--dry-run]
```

**Implementation.** [vault/tools/tropo-register-kernel.py](../vault/tools/tropo-register-kernel.py)

**UID.** [`b3f9d7e2`](../vault/files/b3f9d7e2.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## rehydrate — Tool

**Domain.** Rehydrate vault into navigable folder trees — symlinks under 00-tropo-nav/ for active/archived/all views.

**When to reach for it.** Reach for this when the navigable folder tree at 00-tropo-nav/ is stale or missing — typically after running rebuild-vault.py to update the index + project-tree. The symlink hierarchy is the human-navigable surface; rebuild-vault.py rebuilds the data; rehydrate.py rebuilds the navigation. Run rehydrate.py whenever you want the project hierarchy in your editor's file tree to reflect current vault state. The active/archived/all views let you collapse archived noise without losing it.

**Invocation.**
```
python3 vault/tools/tropo-rehydrate.py [--output-dir-name DIR] [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-rehydrate.py](../vault/tools/tropo-rehydrate.py)

**UID.** [`a7c5e3f1`](../vault/files/a7c5e3f1.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## render-boards

**Domain.** render-boards.py — render board.md files from board-definition + substrate.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/81e168d6.py
```

**Implementation.** [vault/tools/81e168d6.py](../vault/tools/81e168d6.py)

**UID.** [`81e168d6`](../vault/files/81e168d6.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## render-crew-brief

**Domain.** render-crew-brief.py — Auto-render the crew tables in 00-crew-brief.md.

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**Invocation.**
```
python3 vault/tools/6510afc7.py
```

**Implementation.** [vault/tools/6510afc7.py](../vault/tools/6510afc7.py)

**UID.** [`6510afc7`](../vault/files/6510afc7.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## run-rag-pipeline

⚠️ **WARNING — `trigger_description:` missing from vault entry.** Catalog cannot show when-to-invoke prose without this field.

**UID.** [`91c8c388`](../vault/files/91c8c388.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## scan-import-state.py — Boot-Time Import State Scanner

**Domain.** Boot-time anomaly detection for the import primitive — sub-50ms shallow scan that categorizes Studio-root entries + flags whether sa.reconciler should be commissioned this session.

**When to reach for it.** Boot-time shallow scanner for orphan/anomaly detection.

**Invocation.**
```
python3 vault/tools/tropo-scan-import-state.py [--output-dir <dir>]
```

**Implementation.** [vault/tools/tropo-scan-import-state.py](../vault/tools/tropo-scan-import-state.py)

**UID.** [`0a316ca6`](../vault/files/0a316ca6.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-backfill-styles

**Domain.** Backfill v1.28.0 substrate fields onto pre-v1.28.0 entries: original_styles: on external-artifact projections + missing folder-marker mirrors. Closes the migration gap for pre-existing imports.

**When to reach for it.** Reach for this when migrating pre-v1.28.0 substrate up to v1.28.0+ shape. THREE modes: (a) `--projection <uid>` backfills original_styles: on a single external-artifact entry. (b) `--all --yes` sweeps all external-artifact entries without original_styles:; populates them by opening the source binary + calling office_styles.extract_office_styles(). (c) `--folder-markers --yes` (v0.5 NEW) sweeps on-disk .tropo-folder.md markers without matching vault mirrors at vault/files/<marker-uid>.md; co-authors the missing mirror using retro-fill semantics per arch-spec §3.5.5 Amendment 1 v0.5. Sweep modes require --yes to bypass TTY prompt; bare sweep on non-TTY refuses (per spec §3.13 v0.5 precondition 4). Idempotent unless --force. Mike

**Invocation.**
```
python3 vault/tools/tropo-backfill-styles.py (--projection <uid> | --all | --folder-markers) [--yes] [--force]
```

**Implementation.** [vault/tools/tropo-backfill-styles.py](../vault/tools/tropo-backfill-styles.py)

**UID.** [`09ef9843`](../vault/files/09ef9843.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-disposition — backlog disposition harness (safe + bounded)

**Domain.** Governance — safe bounded backlog disposition; MECHANICAL-CERTAIN dispositions handled deterministically; JUDGMENT cases surfaced for review.

**When to reach for it.** Dispose of stale backlog items safely: archives referenced items, recycles unreferenced ones, surfaces judgment cases for review. Default dry-run — add --apply to execute.

**Invocation.**
```
python3 vault/tools/tropo-disposition.py --owner <owner> [--days N] [--apply]
```

**Implementation.** [vault/tools/tropo-disposition.py](../vault/tools/tropo-disposition.py)

**UID.** [`992ba026`](../vault/files/992ba026.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-export

**Domain.** Export an agent-edited markdown working-copy back to a .docx deliverable. The deliverable side of the v1.28.0 import → work → export loop. Two paths: preservation (source binary as scaffold; max fidelity) vs transformation (registered template as scaffold).

**When to reach for it.** Reach for this when a user wants to ship an agent-edited working-copy back as a .docx deliverable. Default invocation (no --template flag) = PRESERVATION export: open the working-copy

**Invocation.**
```
python3 vault/tools/tropo-export.py --working-copy <uid> [--template <uid>] [--output-path <path>] [--title <string>] [--accept-binary-drift]
```

**Implementation.** [vault/tools/tropo-export.py](../vault/tools/tropo-export.py)

**UID.** [`ce9dbcc2`](../vault/files/ce9dbcc2.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-extract.py — Extract markdown working-copy from imported .docx

**Domain.** Extract a markdown working-copy from an imported .docx. The bridge between v1.25.0 binary governance and v1.28.0 agent-native editing — opens the .docx, unpacks word/document.xml, walks paragraphs producing structured markdown, surfaces extraction stats.

**When to reach for it.** Reach for this when a user or agent wants to read + edit the content of an imported .docx in markdown. Resolve the projection UID first (via the imported folder

**Invocation.**
```
python3 vault/tools/tropo-extract.py --projection <uid> [--force] [--working-uid <uid>]
```

**Implementation.** [vault/tools/tropo-extract.py](../vault/tools/tropo-extract.py)

**UID.** [`561d3c75`](../vault/files/561d3c75.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-recycle — Soft-delete governed vault entries

**Domain.** Soft-delete gesture for vault/files/ entries.

**When to reach for it.** Soft-delete governed entries (mv to recycle/). Never use rm.

**Invocation.**
```
python3 vault/tools/tropo-recycle.py
```

**Implementation.** [vault/tools/tropo-recycle.py](../vault/tools/tropo-recycle.py)

**UID.** [`2573f6dd`](../vault/files/2573f6dd.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-register-template

**Domain.** Register a user-uploaded Word template as a governed docx-template. The format-reference half of the v1.28.0 export gesture — pairs with tropo-export.py --template <uid> for transformation export.

**When to reach for it.** Reach for this when a user wants to register a Word template for transformation-export use. The user supplies the .docx path + a lowercase-hyphenated slug ([a-z0-9-]+). Tool copies the binary to .tropo-studio/templates/<slug>.docx, extracts style metadata via the shared office_styles library, and authors a governed docx-template entry. Slug-collision discrimination: 

**Invocation.**
```
python3 vault/tools/tropo-register-template.py --path <path-to-docx> --name <slug> [--description "..."] [--use-case "..."] [--force]
```

**Implementation.** [vault/tools/tropo-register-template.py](../vault/tools/tropo-register-template.py)

**UID.** [`1056eec5`](../vault/files/1056eec5.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-restore — governance-aware git checkout + re-index

**Domain.** Git Beat 1 governed undo (dev-spec 98b9610a)

**When to reach for it.** Restore a governed artifact to a prior git commit with mandatory re-index.

**Invocation.**
```
python3 vault/tools/tropo-restore.py <uid> --to <commit> --reason <text>
```

**Implementation.** [vault/tools/tropo-restore.py](../vault/tools/tropo-restore.py)

**UID.** [`b7e4a2c9`](../vault/files/b7e4a2c9.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-test — Substrate Health CLI

**When to reach for it.** Single-gesture green/yellow/red substrate health verdict.

**Invocation.**
```
python3 vault/tools/tropo-test.py [--quick] [--json] [--verbose]
```

**Implementation.** [vault/tools/tropo-test.py](../vault/tools/tropo-test.py)

**UID.** [`3086287a`](../vault/files/3086287a.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## tropo-validate — Tool

**Domain.** Structural vault validator — registry integrity, UID consistency, orphan detection, AGENTS.md coverage, cross-ref resolution.

**When to reach for it.** Comprehensive read-only audit of vault structural health.

**Invocation.**
```
python3 vault/tools/tropo-validate.py [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-validate.py](../vault/tools/tropo-validate.py)

**UID.** [`d2b9c8e6`](../vault/files/d2b9c8e6.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## validate-canonical-l0 — Tool

**Domain.** Verifies the rendered project-tree's L0 set matches the canonical L0 declaration; fails on drift.

**When to reach for it.** Reach for this when about to make any architectural call that could change the project-tree

**Invocation.**
```
python3 vault/tools/tropo-validate-canonical-l0.py [--vault-path PATH] [--json]
```

**Implementation.** [vault/tools/tropo-validate-canonical-l0.py](../vault/tools/tropo-validate-canonical-l0.py)

**UID.** [`e3f5a8c1`](../vault/files/e3f5a8c1.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## validate-capability-membership — Tool

**Domain.** Validate the typed capability ↔ subsystem membership graph; enforces Checks 19-23 + Rule 11/12 + structural-consistency.

**When to reach for it.** Reach for this when verifying typed-graph integrity — every capability has exactly one subsystem hub in member_of:, every release plan declares capabilities_touched + hub_summaries, every release entry

**Invocation.**
```
python3 vault/tools/tropo-validate-capability-membership.py [--soft] [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-validate-capability-membership.py](../vault/tools/tropo-validate-capability-membership.py)

**UID.** [`f5e2d1c7`](../vault/files/f5e2d1c7.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## validate-no-absolute-paths — Tool

**Domain.** Vault portability validator — fails if any committed file contains absolute machine paths (/Users/.../, ~/...).

**When to reach for it.** Reach for this whenever verifying vault portability — that no committed file has absolute machine paths leaking in (typical contamination: log artifacts, cached output, agent output that captured a /Users/<account>/ prefix). Run as part of build-release.py pre-flight (the build is supposed to ship a portable artifact). Also useful before commits that touched generated content. The vault

**Invocation.**
```
python3 vault/tools/tropo-validate-no-absolute-paths.py [--strict] [vault-root]
```

**Implementation.** [vault/tools/tropo-validate-no-absolute-paths.py](../vault/tools/tropo-validate-no-absolute-paths.py)

**UID.** [`c9b7d4e2`](../vault/files/c9b7d4e2.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## validate-release-manifest — Tool

**Domain.** Validate the ship-artifact manifest graph — 23 checks per ship-artifact.capsule v1.1.4.

**When to reach for it.** Reach for this when verifying the ship-artifact manifest is structurally clean — every ship-artifact entry has valid frontmatter, parent UIDs resolve, output_path is unique, supersession pairs are bidirectional, override-child shape is legal. Run before build-release.py and after any ship-artifact entry edit. Importable shape (`from validate_release_manifest import validate_manifest`) lets build-release.py call it programmatically without subprocess overhead. Pairs with validate-canonical-l0 + validate-capability-membership + tropo-validate as the structural-validator quartet.

**Invocation.**
```
python3 vault/tools/tropo-validate-release-manifest.py [--vault-path PATH]
```

**Implementation.** [vault/tools/tropo-validate-release-manifest.py](../vault/tools/tropo-validate-release-manifest.py)

**UID.** [`e1d8c3a7`](../vault/files/e1d8c3a7.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## vault-search — Multi-field vault index search

**Domain.** vault-search.py — Multi-field vault index search for crew agents.

**When to reach for it.** The lookup verb. Find any tool/skill/entry not on the belt.

**Invocation.**
```
python3 vault/tools/tropo-vault-search.py
```

**Implementation.** [vault/tools/tropo-vault-search.py](../vault/tools/tropo-vault-search.py)

**UID.** [`943149d4`](../vault/files/943149d4.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

## write-activation-entry — Open/close activation registry entries

**Domain.** Companion script for write-activation-entry.skill (UID 7a3d04bc).

**When to reach for it.** Open/close agent activation entries (boot and retirement).

**Invocation.**
```
python3 vault/tools/40b2f455.py
```

**Implementation.** [vault/tools/40b2f455.py](../vault/tools/40b2f455.py)

**UID.** [`40b2f455`](../vault/files/40b2f455.md) | governed by [`tool.capsule v1.4`](capsules/tool.capsule.md)

---

*Tropo Tool Catalog | Generated 2026-07-11 | v1.15 substrate*
